# Tree of Support (Дерево Опоры / Roots & Fruits)

> Личная карта самоопоры: на чём стою, что у меня есть, куда иду.
>
> **Слоган**: Опора — не ресурс, а ресурс — не цель.

---

## 0. Как читать этот документ

Это **единый источник правды** для проекта. Любая Claude-сессия, любой разработчик начинает с него.

Если что-то противоречит — этот документ выигрывает. Если в коде сделано иначе чем здесь — либо чинить код, либо обновлять документ, **с явным обновлением раздела "Decision log"**.

**Структура:**
1. Стратегия и контекст (что и зачем)
2. Продукт и UX (что строим)
3. Технические детали (как строим)
4. Метрики и монетизация (как измеряем)
5. Бэклог и roadmap (куда движемся)
6. Архив (откуда пришли)

---

## 1. Mission, Audience, Voice

### Mission

Помочь человеку **услышать себя** в мире, который непрерывно подсказывает ему чужие цели. Сделать практику самоопознания, которая опирается на собственный метод автора и не требует постоянного присутствия коуча.

Это не приложение для расслабления, не todo-приложение, не дневник эмоций. Это **карта для размышления о себе** с конкретной структурой (4 слоя), доступная для возврата.

### Target audience

Holiday-юзеры на старте — **русскоязычная аудитория 25-40 лет**, склонная к рефлексии, осознающая, что "живёт чужими целями". Через 6-12 месяцев — английский рынок аналогичной аудитории.

**Три ключевые боли, которые решаем:**
1. *"Я живу чужими целями и не понимаю, чего хочу сам"* — главная, отличает нас от конкурентов
2. *"Ставлю цели, но не достигаю/бросаю"* — связь цель↔ресурс↔опора
3. *"Внутри тревога, шатает, нет опоры"* — корни как точка возврата

Боль №1 — главный месседж для лендинга и сторов. Она массовая и слабо закрыта существующими продуктами.

### Voice

- **"Ты"**, не "вы" — теплее, близко, как разговор с собой
- **Спокойный, не назидательный** — продукт уважает темп юзера
- **Конкретный, без эзотерики** — "корень" объяснён через функцию ("точка возврата"), а не через "энергию"
- **Допускает паузу и тишину** — не давит уведомлениями, не геймифицирует инсайты
- **Признаёт что не идеально** — "раскрывается полнее когда вдумчиво", а не "работает только если"

### Author

**Вячеслав** — коуч по самоопоре. Создатель метода и продукта. Имеет Telegram-канал @snowvb. В продукте присутствует в одном месте — блок "От автора" в конце страницы "О подходе".

Личный бренд встроен в продукт намеренно, но мягко: дерево несёт **метод**, не **личность**. Если юзер захочет к автору лично — найдёт через Telegram. Если нет — продукт работает самостоятельно.

---

## 2. Product Principles (Non-Negotiables)

Это правила, которые НЕ нарушаем без явного решения и обновления документа.

1. **Privacy first.** Содержимое дерева глубоко личное. Никогда не индексируется, не шарится без явного действия юзера, не используется в обучающих данных без opt-in.
2. **No addiction patterns.** Не daily-streak-приложение. Не геймифицируем инсайты ("ты на 73% к озарению!"). Не оптимизируем session duration в плюс.
3. **No popups, no upsells in the middle of work.** Pop-ups только на logical break-points (после первого плода, после возврата). Никогда — во время работы с деревом.
4. **Respect emotional weight.** Это инструмент рефлексии. Анимации, тосты, копирайт — всё спокойное. Никаких "Wow! Great job!" Никаких эмодзи в систематических сообщениях.
5. **The user's text is sacred.** Никогда не модифицируем, не "улучшаем" формулировки юзера. Орфография — его, не наша.
6. **Mobile is a first-class citizen** (в Production, не в прототипе) — будут пользоваться с телефона на ходу или дома на диване.
7. **Russian first, English second.** До поиска PMF — только русский. Архитектурно готовы к i18n с первого дня. Один домен (`rootsnfroots.com`), русский на корне без префикса сейчас, `/en/` добавится позже — не два отдельных сайта/домена — см. §9 "Domain & i18n strategy" и D25.

---

## 3. User Journey

### CJM — первый визит

1. Открывает (через ссылку, рекламу, шеринг друга)
2. **Welcome-модалка**: слоган + введение (~90 слов) + кнопка "Понятно, начнём"
3. Видит **пустое дерево** + правую панель с 4 кнопками "Добавить"
4. Кликает `i` рядом с любой кнопкой → **popover с определением** + ссылка "Показать пример"
5. Опционально открывает пример → видит **modal с примером** ("Корень 'шапка'", "Ресурс 'терпение'" и т.д.)
6. **Заполняет любую из зон** — порядок не навязываем
7. При **первом плоде** → **диалог "На какой срок ты сейчас смотришь?"** (6m / 1y / 3y, можно пропустить)
8. Открывает **левую шторку с вопросами** для рефлексии — табы по зонам, 3 уровня глубины
9. Опционально читает **"О подходе"** (через подвал или меню)

### CJM — возврат

1. Открывает, видит дерево в том состоянии в каком оставил
2. **Welcome НЕ показывается** (флаг `tree_intro_seen_v1` в localStorage)
3. Может **отметить плоды как "сорванные"** через клик → меню → "Сорвать плод"
4. Может **поменять период реализации** через меню или pill-кнопку в шапке
5. Может **перетащить любой элемент** мышкой (опоры, ресурсы, плоды)

### CJM — конец сессии

Юзер закрывает вкладку. Все данные в localStorage. При следующем открытии — продолжит с того же места.

В Production-версии: данные синхронизированы с Supabase, доступны с разных устройств после auth.

---

## 4. Data Model

### Entities

```typescript
// Корень / опора
interface Root {
  id: string;          // uuid
  text: string;
  slotId?: string;     // 'root-1' .. 'root-6' — если из стандартного слота
  x?: number;          // 0-100, если перетащен
  y?: number;
  createdAt: timestamp;
  updatedAt: timestamp;
}

// Ресурс / ствол
interface TrunkItem {
  id: string;
  text: string;
  x?: number;          // 0-100, если pinned
  y?: number;
  pinned: boolean;     // true = юзер перетащил, не пересчитывать автопозицию
  createdAt: timestamp;
  updatedAt: timestamp;
}

// Ветка фокуса
interface Branch {
  id: string;
  text: string;
  slotId: string;      // 'branch-1' .. 'branch-5' (обязательно)
  createdAt: timestamp;
  updatedAt: timestamp;
}

// Плод
interface Fruit {
  id: string;
  text: string;
  x: number;           // 0-100 (всегда заданы)
  y: number;
  harvested: boolean;  // отмечен как достигнутый
  branchId?: string;   // в текущей версии НЕ привязан жёстко, перетащим со временем
  harvestedAt?: timestamp;
  createdAt: timestamp;
  updatedAt: timestamp;
}

// Дерево как контейнер
interface Tree {
  id: string;
  userId: string;
  name: string;        // 'Моё дерево' по умолчанию
  horizon?: '6m' | '1y' | '3y';
  roots: Root[];
  trunk: TrunkItem[];
  branches: Branch[];  // макс 5
  crown: Fruit[];
  createdAt: timestamp;
  updatedAt: timestamp;
}

// Юзер
interface User {
  id: string;
  email?: string;       // null для анонимов
  tier: 'free' | 'premium';
  subscriptionStatus?: 'active' | 'past_due' | 'canceled';
  language: 'ru' | 'en';
  createdAt: timestamp;
  lastActiveAt: timestamp;
}
```

### Constraints

- Roots: до 6 в slotах; больше — спавнятся возле земли (y=88) с offset
- Branches: **жёсткий лимит 5**, попытка добавить 6-ю → toast "Сейчас 5 из 5..."
- Trunk: без лимита, новые спавнятся в центр круга с лёгкой спиралью
- Fruit: без лимита, спавнятся на свободных позициях апельсинов из `ORANGE_POSITIONS`

### Storage

**Прототип:** localStorage с ключом `treeItems_v6`, `tree_intro_seen_v1`, `tree_horizon_v1`.

**Production:** Supabase Postgres. Auth через Supabase. Анонимные сессии (до регистрации) хранятся в localStorage, при регистрации мигрируем в БД.

---

## 5. Tree Coordinates (в процентах от размера картинки)

```typescript
const ROOT_SLOTS = [
  { id: 'root-1', x: 25, y: 94 },
  { id: 'root-2', x: 36, y: 95 },
  { id: 'root-3', x: 47, y: 95.5 },
  { id: 'root-4', x: 58, y: 95.5 },
  { id: 'root-5', x: 69, y: 95 },
  { id: 'root-6', x: 80, y: 93 },
];

const BRANCH_SLOTS = [
  { id: 'branch-1', x: 48, y: 36, angle: 0 },    // Верх-центр
  { id: 'branch-2', x: 28, y: 42, angle: -25 },  // Лево-верх
  { id: 'branch-3', x: 72, y: 42, angle: 25 },   // Право-верх
  { id: 'branch-4', x: 26, y: 67, angle: 25 },   // Лево-низ
  { id: 'branch-5', x: 72, y: 67, angle: -25 },  // Право-низ
];

const ORANGE_POSITIONS = [
  { x: 50, y: 15 }, { x: 35, y: 12 }, { x: 65, y: 12 },
  { x: 20, y: 25 }, { x: 80, y: 25 },
  { x: 12, y: 35 }, { x: 88, y: 35 },
  { x: 18, y: 48 }, { x: 82, y: 48 },
  { x: 10, y: 22 }, { x: 90, y: 22 },
  { x: 28, y: 18 }, { x: 72, y: 18 },
  { x: 25, y: 40 }, { x: 75, y: 40 },
];

const TRUNK = { centerX: 50, centerY: 48, radius: 16 };
```

Картинка дерева: `tree.png` (1179×786 px). Координаты в процентах для resolution-independence.

---

## 6. Design System

### Colors

```css
--ink:        #2B2A26;    /* основной текст */
--ink-soft:   #5C5A53;    /* вторичный текст */
--ink-muted:  #8A877E;    /* подписи, captions */
--cream:      #FDFBF7;    /* фон правой панели и страниц */
--cream-2:    #F5F1E8;    /* hover-state поверхности */
--line:       #E8E2D4;    /* бордеры */
--line-soft:  #F0EBE0;    /* лёгкие бордеры в списках */

--terracotta:      #B8896B;  /* опоры основной */
--terracotta-dark: #8B6344;  /* опоры тёмный (текст/акценты) */
--moss:            #7A8D6A;  /* ресурсы основной */
--moss-dark:       #5B6E4E;  /* ресурсы тёмный */
--sage:            #AED581;  /* ветки фокуса основной */
--sage-dark:       #8BAF5C;  /* ветки фокуса тёмный */
--coral:           #E8876B;  /* плоды основной */
--coral-dark:      #C76A4F;  /* плоды тёмный */

--accent:     #6B8FA8;  /* нейтральный сине-серый для интерактивных элементов */
```

Палитра намеренно **приглушённая, землистая**. Не crayola. Сатурация ниже среднего. См. Lessons Learned про эволюцию от ярких к нынешним тонам.

### Typography

- **UI / тело**: Manrope (400 / 500 / 600). Через Google Fonts (NB: без `opsz` axis, см. Lessons Learned).
- **Заголовки, бренд, акценты**: ~~Fraunces~~ **Literata** (400 / 500 / 600, italic). Тёплый humanist book-serif. Заменили Fraunces на этапе Component library — см. D24, Fraunces не имеет кириллических глифов. Один шрифт для ru и en, не разный по локали.
- **Шкала**: 28 / 20 / 15 / 13 / 12 / 11 px
- **Веса**: 400 для тела, 500 для UI, 600 для акцентов и uppercase-меток. **Не используем 700+** — слишком напористо для wellness-контекста.

### Spacing, radii, shadows

- **Радиусы**: только 8px (компоненты) и 16px (карточки) + 999px (pill-кнопки для веток фокуса)
- **Тени**: 3 уровня — subtle / standard / elevated (см. CSS-переменные)
- **Motion**: 150-300ms, easing `cubic-bezier(0.4, 0, 0.2, 1)` для всего. Лишний bounce не используем.

### Tree image background

Фон сцены — `tree.png` через `background-image: cover` с `backdrop-filter: blur(30px) brightness(0.6) saturate(1.05)` поверх. Главное изображение дерева — `<img>` с `object-fit` контейнером для координатных overlay. См. раздел Lessons Learned об эволюции этого решения.

---

## 7. Texts (Source of Truth)

> Этот раздел — **полный текст всех копирайтов**. Если в коде иначе — копируй отсюда.

### 7.1 Slogan (всегда виден в шапке)

> *Опора — не ресурс, а ресурс — не цель.*

### 7.2 Welcome Modal (первый вход, флаг `tree_intro_seen_v1`)

**Заголовок:** Дерево Опоры
**Подпись:** Опора — не ресурс, а ресурс — не цель *(italic, мелко)*

**Тело:**
> Большинство из нас живёт целями, которые подсказали извне — родители, культура, лента. Внутренний компас при этом замолкает и ослабевает связь с собой. Это дерево — способ услышать его снова и восстановить контакт с внутренним миром.
>
> Здесь ты увидишь себя с разных сторон: на чём ты стоишь, что у тебя уже есть, куда направлено внимание, и какие плоды действительно твои.
>
> Заполняй медленно, вдумчиво. Прислушивайся к телу — там, где приятный отклик, и есть истинно твоё.
>
> *(в выделенном блоке с terracotta-линией слева)*: Как и настоящее дерево: крона не вырастет шире, чем позволяют корни, а слабый ствол не выдержит вес плодов.
>
> Возвращайся к дереву. Реальность меняется, и дерево меняется вместе с ней. Так оно остаётся живым, а не превращается в галочку.

**Кнопка:** Понятно, начнём

### 7.3 Zone Definitions (popover при клике `i`)

#### Корни — Опоры (твоя точка возврата)

> То, что возвращает тебе ощущение опоры и тепла, когда внутри тревога, упадок или сомнение. Люди, события, ценности, символы — всё, что напоминает: «я уже справлялся, у меня есть на что опереться». Корни не решают проблему — они возвращают спокойствие, из которого можно действовать дальше.

#### Ствол — Ресурсы (то, что у тебя есть сейчас)

> Реальные ресурсы, которые работают на тебя последние 3 месяца и будут доступны ещё 3 месяца вперёд. Деньги, навыки, время, отношения, опыт. Не «как должно быть», а как есть — включая то, что хотелось бы изменить. Ствол даёт чувство собственной ценности и делает путь к плодам видимым.

#### Ветки фокуса (куда ты направляешь энергию)

> Сферы, в которых ты хочешь расти прямо сейчас: спорт, отношения, бизнес, семья, обучение. У человека ограничено внимание — поэтому веток немного, до пяти. Ветка — это не цель. Это направление фокуса, в котором будут расти твои плоды.

#### Плоды (конкретные шаги)

> То, что ты собираешься сорвать. Не «хочу больше денег», а «200к в месяц к декабрю» — это главный плод. И промежуточные — как шаги, без которых невозможно сорвать главный. Чем конкретнее формулировка — тем выше вероятность успеха и вкус плода, даже если остановишься на промежуточном.

### 7.4 Examples (в modals при клике "Показать пример")

#### Пример: Корень «шапка» или «семья»

> Корнем может быть как что-то конкретное (предмет, человек, событие), так и абстрактное — семья, юмор, дружба, свобода. Конкретика не важна. Важно чувство, которое возникает, когда смотришь на этот корень.
>
> Слово «шапка» у разных людей значит разное:
> — У одного — событие: ребёнком увидел шапку и так впечатлился, что теперь сам делает шапки.
> — У другого — человек: ассоциация с близким, при взгляде встаёт его образ.
> — У третьего — символ: ощущение себя в моменте, когда надевал шапку, например любовь к маме.
>
> Главное не само слово, а отклик внутри.

#### Пример: Ресурс «терпение» или текущий доход

> Одно слово — разная функция. «Терпение»:
> — Один записал в смысле «не спешить, дать процессу время». Когда видит — успокаивается.
> — Другой — как напоминание: раньше терпел во вред себе, теперь учится не молчать.
>
> Текущий доход тоже звучит по-разному:
> — Для одного это средство: возможность вложиться в курс, переезд, плод.
> — Для другого — достижение: видимый результат пути.
>
> Когда ствол заполнен — должно быть ясно видно, как именно эти ресурсы помогут сорвать каждый плод.

#### Пример: Ветка «Бизнес» — две версии

> Одна и та же ветка, разные намерения:
> — У одного «Бизнес» — почитать про рынок и подумать, в какую нишу заходить.
> — У другого «Бизнес» — найти первого клиента и закрыть сделку до конца месяца.
>
> Плоды отражают основное намерение развития ветки — то, куда ты тратишь свой драгоценный фокус, время и ресурсы. Поэтому название ветки лучше делать конкретным: не «Здоровье вообще», а «Восстановление сна» или «Силовые тренировки».

#### Пример: Плод — формулировка решает

> «Хочу больше денег» → мозг не понимает, что делать. Цель размыта.
> «200к в месяц к декабрю, чтобы переехать» → есть срок, сумма, смысл.
>
> Если ветка «Смена профессии», мастер-плод может быть «устроиться на новую работу к июню». Промежуточные:
> — Изучить рынок (и заранее решить, как поймёшь, что плод сорван).
> — Поговорить с человеком из этой профессии.
> — Пройти курс по нужному навыку.
>
> Так большая цель разбивается на достижимые шаги.

### 7.5 Questions for Reflection (левая шторка)

Структура: 3 уровня глубины в каждой зоне.

#### Опоры

**С чего начать:**
- Без чего ты не представляешь свою жизнь?
- Что или кто всегда был тебе опорой?

**Чтобы копнуть глубже:**
- На что ты опираешься столько, сколько себя помнишь?
- Что поддерживало тебя в самые трудные моменты — и помогает до сих пор?
- Кто или что было рядом в моменты, когда ты стал сильнее?

**Тело подсказывает:**
- Когда ты думаешь о том, что тебя по-настоящему держит на плаву — где в теле появляется тепло, спокойствие, единение? Если отклика нет — возможно, эта опора не твоя или больше не актуальна.

#### Ресурсы

**С чего начать:**
- Какие свои качества, достижения считаешь поводом для гордости?

**Чтобы копнуть глубже:**
- Какие ресурсы помогут тебе продвигаться ближе к вкушению плодов по каждой из веток?
- Какие ресурсы сейчас актуальны и будут ещё как минимум 3 месяца?
- Есть ли какое-то качество, тревога или ситуация, которая ощущается как груз? Как видишь её трансформацию — и что в ней работает на тебя уже сейчас, в том виде, какое есть?

**Тело подсказывает:**
- При взгляде на ресурсы — какие вызывают ощущение «я могу», а какие напряжение или сжатие? Это сигнал: одни поддержать и усилить, к другим изменить отношение, увидеть силу и принять.

#### Ветки фокуса

**С чего начать:**
- Если бы у тебя была одна сфера, на которой нужно сосредоточиться полгода — какая?

**Чтобы копнуть глубже:**
- Топ каких сфер жизни больше всего хочешь изменить, улучшить, вырасти в ближайшие месяцы или год? Выбери каждой приоритет от 1 до 5, ориентируясь на доступные ресурсы.
- Какие ветки уже получают твоё внимание и энергию, даже если ты этого не замечаешь?
- Какие ветки сейчас требуют твоего участия больше всего — куда хочешь направить ресурсы?

**Тело подсказывает:**
- Какие основные векторы в твоей жизни самые важные — прочти их название и следи за откликом тела, где замечаешь отклик? Если отклика нет — возможно, желание не лично твоё, а взято извне.

#### Плоды

**С чего начать:**
- Какой ты хочешь видеть свою жизнь через год? Подумай и представь свои дни по каждой из приоритетных сфер.

**Чтобы копнуть глубже:**
- Какой плод самый важный на каждой ветке — тот, что подарит желаемую реальность?
- Какие конкретные шаги (промежуточные плоды) необходимо вкусить, чтобы сорвать основной?
- Какие ресурсы из ствола помогут тебе сорвать плоды?

**Тело подсказывает:**
- Когда ты представляешь, что плод сорван — какое ощущение? Радость и спокойствие — твой. Тревога и пустота — возможно, цель не твоя.
- Какими ощущениями откликается в теле общий взгляд на крону целиком?

### 7.6 Horizon Dialog (при первом плоде)

**Header (label):** "Перед тем как добавить плод" *(в первый раз)* / "Период реализации" *(при смене)*

**Title:** На какой срок ты сейчас смотришь?

**Body:**
> Прежде чем выбрать, прикинь — это поможет остаться в реальности и не напланировать слишком много.
>
> *(в подсвеченном блоке):*
> — Какой у тебя сейчас ритм — спокойный или загруженный? Сколько часов в неделю реально остаётся на новое?
> — Какие планы уже есть — переезды, проекты, обязательства? Что из этого съест внимание?
> — Сколько времени нужно на отдых и восстановление, чтобы не выгореть?
>
> *(italic):* Лучше взять срок с запасом, чем переоценить силы. В среднем люди недооценивают сроки в 1.5–2 раза.

**Опции:**
- 6 месяцев — короткий цикл, быстрая обратная связь
- 1 год — привычный горизонт планирования
- 3 года — долгий путь, серьёзные изменения

**Footer:** Возвращайся к дереву. Ствол и крона меняются вместе с твоей реальностью. Период реализации тоже можно поменять в меню.

**Skip:** "Пропустить" / "Отмена"

### 7.7 Toast при 5/5 веток

**Title:** Сейчас 5 из 5
**Body:** Удали одну ветку, чтобы добавить новую — фокус не должен размываться.

### 7.8 Hint в правой панели

**Header:** Подсказка
**Body:** Кликни `i` чтобы узнать про каждый тип. Опоры, ресурсы и плоды можно перетаскивать мышкой.

### 7.9 About page (О подходе)

**Заголовок:** О подходе
**Slogan-подпись над заголовком:** *Опора — не ресурс, а ресурс — не цель*

#### Раздел: Зачем дерево

> Нас не учат слышать себя. Мы живём по требованиям и ожиданиям мира — и постепенно учимся игнорировать собственные сигналы ради внешнего. Это дерево помогает увидеть себя с разных углов и придумать, как проявить это понимание в реальной жизни.
>
> Дерево — это не цель, это *отпечаток*. На уровне головы и эмоций ты смотришь на себя через осознанность и вопросы, формулируешь путь к тому, что приближает тебя к счастью. Реальный путь может отличаться — но твой главный инструмент, мозг, теперь знает твои настоящие желания. На пути ты сможешь опираться на эти мысли и чувства, когда станет сложно или страшно.

#### Раздел: Как устроено дерево

> *(в выделенном блоке terracotta):* Крона не вырастает шире, чем позволяют корни. Ствол не может включать ресурсы, выходящие за пределы того, каким ты себя ощущаешь. Корни не питают конструкцию, если ты не видишь, как опираешься на них в разных ситуациях. Всё связано.

> **Корни** — то, на что ты опирался, опираешься и будешь опираться. Родители, друзья, опыт боли, после которой ты стал сильнее. В домиках внизу дерева ты собираешь эти опоры, видишь, как они помогали, замечаешь связь с твоими мечтами.

> **Ствол** держится прочно за счёт корней. Это твои актуальные ресурсы: победы, поражения, работа, деньги, качества, которые укрепляют опору на себя — те, которыми гордишься и открыто проявляешь, и те, что прячешь или хочешь трансформировать. Ствол — это видимая, честная картина «что у меня сейчас есть, без деления на хорошо или плохо, просто как есть, без оценок».

> **Крона** растёт настолько, насколько ты ухаживаешь за корнями и направляешь свет внимания на дерево. Корни уходят глубже, когда ты замечаешь моменты опоры. Крона расширяется, когда ты признаёшь свои ресурсы. Плоды зреют, когда ты понимаешь: «у меня есть или будет время, желание и ресурс для этого».

#### Раздел: На каких подходах основано

**Карточка 1: ACT — Acceptance & Commitment Therapy**
> Терапия третьей волны: принятие, осознанность, ясность ценностей, действие из ценностей. Корни ↔ ценности, Ствол ↔ принятие реальности, Ветки и Плоды ↔ committed action. Мета-анализ 65 исследований (Springer, 2025) показал умеренный эффект ACT (Hedges' g = 0.72) на снижение тревоги и депрессии и рост психологической гибкости.

**Карточка 2: Implementation Intentions**
> Формулировка целей в формате «когда X, то Y» удваивает шанс их достижения. Мета-анализ 94 исследований Голвитцера и Шерана показал эффект d = 0.65 (medium-to-large). Поэтому плоды формулируются конкретно — с местом, временем и ресурсом.

**Карточка 3: Self-compassion (Кристин Нефф)**
> Заполнять дерево нужно с теплом к себе, без самокритики. Мета-анализ 56 RCT (Mindfulness, 2023) подтвердил, что self-compassion интервенции снижают депрессию, тревогу и стресс. Корни — это инструмент самосострадания: «у меня есть на что опереться».

**Карточка 4: Интроспекция и интероцепция — взгляд внутрь**
> Дерево раскрывается полнее, когда заполняешь его, заглядывая внутрь, а не на автомате. Интроспекция — это наблюдение за своими мыслями, ценностями и чувствами; она отвечает на вопрос «что я думаю и чувствую». Интероцепция — способность слышать сигналы тела: «где это живёт и есть ли настоящий отклик». Вместе они защищают от того, чтобы заполнить дерево чужими, заученными ответами. Систематический обзор (Frontiers, 2020) показал: чем выше осознанность тела, тем эффективнее регуляция эмоций. Поэтому в каждой зоне есть вопрос «что говорит тело».

#### Раздел: Дерево живое

> Дерево не делается один раз и навсегда. Реальность меняется — у тебя появляются новые ресурсы, что-то отпадает, фокус смещается. Возвращайся к дереву, когда чувствуешь, что внутри что-то сдвинулось.
>
> Посмотри на ствол: всё ли ещё работает на тебя? Глянь на ветки фокуса — энергия идёт туда же или сместилась? Какие плоды стали ближе, какие отвалились, какие новые проявились?
>
> Это не отчётность. Это способ оставаться в контакте с собой по мере того, как ты меняешься. Дерево, которое отражает реальность, помогает. Дерево-галочка, которое стоит без движения — нет.

#### Раздел: Если хочется глубже

- **ACT в практике** — Russ Harris, «Ловушка счастья»
- **Self-compassion** — Kristin Neff, «Самосострадание»
- **Тело и нервная система** — Deb Dana, «Тело на твоей стороне»
- **Постановка целей** — Peter Gollwitzer, статьи об implementation intentions

#### Раздел: От автора (terracotta блок)

> Я долго жил целями, которые казались моими, а на деле были подсказаны извне — тем, что я "должен" хотеть, тем, как принято, тем, что одобряют рядом. В какой-то момент стало невозможно проживать жизнь дальше.
>
> Один из навыков, которые помогли — отделять своё от чужого. Доверять сигналам тела, давать им приоритет, осознанно выбирать и действовать из внутреннего отклика. Когда учишься слышать себя и учитывать, начинаешь видеть, как это меняет жизнь к лучшему — и тогда самому хочется светить фонариком в тёмные углы вместо того, чтобы их избегать, даже если избегать по привычке комфортно. И тогда становится сложнее обманывать себя и делать вид, что всё как хочется — яснее прорисовываются желания, потребности, ценности. Так началась более полная жизнь — вместо её проживания. Когда впервые ощущаешь эту разницу в качестве жизни и ежедневном состоянии, начинаются знакомство с собой на глубинном уровне и отношения с собой как с опорой.
>
> Мне важно, чтобы у людей был способ думать и решать самим — не вместо терапевта, не вместо коуча, а как личная практика, в которую можно вернуться, когда захочется. Если оно сработает для тебя — буду рад.
>
> Если дерево откликнулось и захочется глубже — пиши, я рядом: [@snowvb](https://t.me/snowvb)
>
> *— Вячеслав, коуч по самоопоре*

### 7.10 Menu items (правая панель, иконка `⋯`)

- Снова показать введение
- Задать период реализации / Поменять период реализации
- О подходе
- ---
- Экспорт данных
- Очистить дерево *(red)*

### 7.11 Fruit context menu (клик по плоду)

- Редактировать
- 🍂 Сорвать плод *(или "Вернуть (не сорван)" если уже сорван)*
- ---
- Удалить *(red)*

---

## 8. Interactive Behaviors

### 8.1 Drag & Drop

Все элементы Roots / Trunk / Crown — draggable. Branches — НЕ draggable (фиксированные слоты).

Логика:
- `mousedown` на элементе → флаг "dragging" с порогом 3px движения
- Если меньше 3px движения → considered click (open menu / edit)
- При drag: позиция считается в процентах от containerRef (ref на div-обёртку картинки)
- При drop: x, y сохраняются; для Trunk выставляется `pinned: true` (auto-positioning больше не действует)

### 8.2 Harvest mechanic

Клик по плоду → mini-menu (Редактировать / Сорвать / Удалить).

При "Сорвать":
- `harvested: true` записывается
- Плод визуально меняется: цвет → приглушённый коричневый, текст зачёркнут, появляется зелёная галочка ✓, **CSS-маска** добавляет надкус справа-сверху (через `mask-image: radial-gradient`)

Счётчик в правой панели: `✓2 · 5` — сорвано 2 из 5 всего.

### 8.3 Welcome flow

- При первом open приложения (нет `tree_intro_seen_v1` в localStorage) → показывается Welcome modal
- Закрытие → флаг ставится → больше не показывается
- Повторный показ — через меню "Снова показать введение"

### 8.4 Horizon flow

- При первой попытке добавить плод (и если `tree_horizon_v1` не установлен) → диалог горизонта
- После выбора (или пропуска) → продолжается добавление плода
- Pill в шапке правой панели показывает выбранный горизонт
- Клик по pill или меню → можно поменять

### 8.5 Questions drawer

- По умолчанию закрыта
- Вертикальный trigger слева ("Вопросы для рефлексии") — клик открывает
- Внутри: табы (Опоры / Ресурсы / Ветки фокуса / Плоды) + 3 раскрывающиеся секции
- Закрытие — крестик в шапке или клик по фону

### 8.6 Info popovers (`i` rядом с кнопками)

- Клик `i` → popover снизу с определением зоны + ссылка "Показать пример"
- Повторный клик на ту же `i` или клик вне popover → закрыть
- "Показать пример" → закрывает popover, открывает modal с примером

---

## 9. Tech Stack & Decisions

### Production stack

| Слой | Выбор | Почему |
|------|------|--------|
| Frontend | **Next.js 14+** (App Router) | SSR/ISR для SEO, оптимизация шрифтов (next/font), image optimization, лучшая экосистема для React. Vercel-native. |
| Styling | **Tailwind CSS** + CSS variables для design tokens | Скорость разработки, маленький бандл, типобезопасность через class-variance-authority |
| Стейт | **React Context** + **Zustand** для глобального стейта | Простой dev experience, минимальный overhead, легко тестируется |
| Drag & Drop | **dnd-kit** (`@dnd-kit/core`) | Touch-friendly из коробки, доступность, активная поддержка |
| Auth + DB | **Supabase** | Postgres + auth + storage в одном, бесплатный план, edge functions, RLS-полиси |
| Hosting | **Vercel** | Авто-деплой из GitHub, edge network, бесплатно для нашего масштаба |
| Analytics | **PostHog** | Product analytics + session replays + feature flags в одном инструменте, 1M событий/мес бесплатно |
| Error tracking | **PostHog Errors** | Тот же клиент что и аналитика, не нужен второй аккаунт. Client-side only — см. D35 про ограничение и когда пересмотреть на Sentry |
| Payments (later) | **Stripe** | Стандарт, лучшая dev experience |
| Email | **Resend** | Простой API, хорошие deliverability metrics. Подключен раньше плана как custom SMTP для Supabase Auth — см. D28 |
| i18n (later) | **next-intl** | Native Next.js поддержка |

### Принципы архитектуры (Architecture Principles)

1. **Server-first где можно** — данные через Server Components, мутации через Server Actions. Client Components только там, где нужна интерактивность (drag, popovers).
2. **Feature flags везде** — каждая премиум-фича закрыта `featureFlags.has('multi-tree')`. По умолчанию false. Можно включить per-user через PostHog flags без релиза кода.
3. **`isPremium` поле в user model с первого дня** — даже если все пока free, инфраструктура готова к paywall.
4. **No localStorage для critical data в Production** — всё через Supabase. localStorage только для анонимной сессии до auth.
5. **Все CDN-зависимости запинены** — version locks в package.json + lockfile. Не "latest", не `^` без необходимости.
6. **Шрифты — через next/font/google** — оптимизированная загрузка, без runtime сюрпризов от Google Fonts API.
7. **Никакого in-browser компилятора** — JSX компилируется на build-time через SWC (встроен в Next.js).
8. **No console.log в Production** — через ESLint правило. Используем PostHog/Sentry для логирования.
9. **Тесты:** Unit-тесты для бизнес-логики (drag bounds, harvest toggle, slot allocation), E2E через Playwright для критичных flow (welcome → first root → first fruit + horizon).
10. **CI/CD:** GitHub Actions → Vercel preview deploy on PR, auto-deploy main to production.

### Domain & i18n strategy

**Один домен, один Next.js app** — не два отдельных сайта/домена. Русский на корне (`rootsnfroots.com`, без префикса) сейчас — это единственный рынок запуска. `/en/` добавится как путь, когда стартует английская локализация (Backlog B4), не обязательно спустя долгий срок.

- Пока существует только русский контент — путь `/ru/` не нужен, весь контент на корне. Реструктуризация под `next-intl`'s `/[locale]/` роутинг откладывается до появления `/en/` (см. D25 про "не проектировать под гипотетические будущие требования раньше времени").
- **Будущий swap:** когда en-версия дозреет до полноценного использования, домен переворачивается одномоментно (301, не поэтапно) — en на корень, ru на `/ru/`. Внешние ссылки обновляются вручную в момент свопа. Это зафиксированное, но отложенное решение — ничего под него сейчас не резервируется в коде.
- При первом визите — auto-detect языка через `Accept-Language`, с явным переключателем в UI (не молчаливый редирект без возможности отмены).
- Полный rationale — см. D25.

---

## 10. North Star Metric

### Метрика

**Weekly Active Returners (WAR)** — % пользователей, которые **вернулись хотя бы один раз** в течение недели после первого визита.

### Почему именно эта метрика

1. **Wellness-продукт ≠ daily-продукт.** Большинство bullet metrics не подходят (DAU, MAU мерят не то). Дерево предполагает рефлексивные сессии, а не ежедневное использование. Возвращение через неделю — здоровый сигнал ценности.
2. **Возврат = leading indicator PMF.** Если люди возвращаются — у продукта product-market fit, можно строить дальше. Если нет — никакая оптимизация воронки не спасёт, надо менять core.
3. **Анти-метрика к токсичным паттернам.** Не толкает к streaks, push-notifications, dark patterns. Поощряет органическое возвращение.
4. **Совместима с бизнес-моделью.** Возвращающиеся — основа для подписки. Не возвращаются — никакая подписка не сработает.

### Условия смены North Star

Менять основную метрику = серьёзный продуктовый pivot, требует решения совместно (Vyacheslav + Claude как продакт). Триггеры рассмотрения смены:

1. **Если WAR стабильно >40% на 3-месячном горизонте** — продукт явно работает, новая бочка — глубина engagement. Возможна смена на **"Trees Completed"** (полное заполнение) или **"Fruits Harvested per User"**.
2. **Если WAR стабильно <10% несмотря на оптимизации воронки** — продукт не цепляет. Прежде чем менять метрику — нужно понять почему (интервью). Если онбординг чинится — остаёмся. Если ценность не доходит — pivot продукта, не метрики.
3. **Если запускаем premium с conversion > 5%** — добавляем вторичную метрику **MRR**, но WAR остаётся главной leading-метрикой engagement.

### Supporting metrics (всегда мерим)

- **D7 / D30 / D90 retention** разрезанные по точке "содержательного заполнения"
- **Conversion в "первый момент ценности"** (заполнил минимум по 1 элементу в каждой зоне)
- **Conversion в "содержательное заполнение"** (≥3 корня, ≥3 ресурса, ≥2 ветки, ≥3 плода)
- **Conversion в "завершённое дерево"** (≥5/≥5/3-5/≥5)
- **Fruit harvest rate** (% юзеров с ≥1 harvested fruit)
- **About page open rate** (proxy для engagement)
- **Coach link clicks** (proxy для перехода в личную практику)

### Anti-metrics (НЕ оптимизируем вверх)

- **Session duration** — мерим как diagnostic, не как target
- **Number of edits per session** — мерим, но не давим в сторону "больше"
- **Notification engagement** — у нас почти нет уведомлений, и не хотим обратного

---

## 11. Funnel & Conversion Stages

| Стадия | Что значит | Цель (бенчмарк) |
|--------|-----------|-----------------|
| App opened | Юзер открыл первый раз | 100% (baseline) |
| Welcome closed | Дочитал и закрыл welcome | >85% |
| First entity added | Добавил хоть один элемент (любой зоны) | >60% |
| Minimal completion | По 1+ элементу в каждой из 4 зон | **>45%** ← ключевая ступень |
| Substantial completion | ≥3 корня, ≥3 ресурса, ≥2 ветки, ≥3 плода | **>20%** ← момент ценности |
| Returned within D7 | Вернулся через 1-7 дней | **>25% от reached substantial** |
| First fruit harvested | Отметил плод как сорванный | >15% |
| About page opened | Открыл и читал страницу "О подходе" | >10% |

---

## 12. Event Tree (Analytics)

PostHog events. Все события имеют `distinct_id` (user), `timestamp`, и базовые контекстные поля.

### Onboarding events

```
app_opened                     — первый заход
welcome_shown                  — welcome модалка показана
welcome_closed                 — welcome закрыт (props: time_spent_sec)
zone_info_clicked              — клик `i` (props: zone)
example_viewed                 — открыт пример (props: zone)
first_item_added                — первый элемент в зоне (props: zone) — было 4 отдельных события
                                   (first_root_added/first_trunk_added/first_branch_added/first_fruit_added),
                                   объединены в одно с `zone`-property для консистентности с item_added/
                                   item_edited/item_deleted/item_dragged, см. D34
horizon_dialog_shown
horizon_chosen                 — props: value (6m/1y/3y)
horizon_skipped
```

### Engagement events

```
tree_returned                  — props: days_since_last_visit, days_since_signup
fruit_harvested                — props: time_since_added_days
fruit_unharvested              — отмена сорванности
item_edited                    — props: zone
item_dragged                   — props: zone, distance_pct
item_deleted                   — props: zone
about_page_opened
about_section_read             — props: section (если scrolled past)
questions_drawer_opened
questions_zone_viewed          — props: zone
questions_level_expanded       — props: zone, level (start/deep/body)
coach_link_clicked             — клик на @snowvb
horizon_changed                — props: from, to
```

### Funnel signals (composite, computed in PostHog)

```
minimal_completion_reached     — собирается из counts
substantial_completion_reached
full_tree_reached
```

### Tech events

```
error_occurred                 — props: error_msg, location
sync_failed                    — если Supabase sync упал
session_started
session_ended                  — props: duration_sec, actions_count
```

### User properties

```
roots_count
trunk_count
branches_count
crown_count
harvested_count
horizon
tier                           — free/premium
first_visit_at
last_visit_at
tree_created_at
language
```

---

## 13. Monetization Plan

### Phase 1: Free everything (запуск + 2-4 месяца)

- Все фичи доступны всем
- В коде заложены **feature flags** для будущих премиум-фич, но **не активированы**
- В user model — поле `tier`, по умолчанию `free`
- В UI — никаких "Upgrade" кнопок, никаких paywall'ов
- Шеринг-карточка (когда сделаем) — **с вотермаркой** rootsnfroots.com — это вирусный механизм И будущий апселл одновременно

**Цель Phase 1:** валидировать продукт, накопить когорту, отладить core experience, понять что люди реально ценят.

### Phase 2: Premium (через 2-4 месяца, по данным)

**Запуск только когда есть:**
- WAR > 20%
- Substantial completion > 20%
- ~1000+ активных пользователей
- ≥10 проведённых качественных интервью

**Гипотезы для premium-фич** (всё с пометкой "проверить данными перед включением"):

- **Multi-tree** — несколько деревьев (разные горизонты, личное/рабочее)
- **History / timeline** — видеть эволюцию дерева во времени
- **Smart reminders** — напоминания о возвращении (push/email), customizable
- **Advanced questions & somatic practices** — глубокий контент авторства Вячеслава
- **PDF export (красивый)** — без вотермарки, оформленный
- **Share card без watermark**
- **Multiple horizons in one tree** — миксованные плоды
- **Optional: direct access** — мини-курсы или сессии с Вячеславом по запросу

### Цена (гипотеза)

- **$5-7/мес или $30-40/год** — соответствует бенчмаркам ниши
- Скорее всего **$4.99/мес / $29.99/год**  для ru-рынка (на старте)

### Реклама (см. D44) и что мы не делаем

- ❌ **Реклама в основном flow** (дерево, вопросы для рефлексии, диалоги) — это sacred space продукта, убьёт атмосферу wellness-продукта
- ✅ **Нативная/партнёрская реклама в периферийных зонах** (низ "О подходе", будущий блог, лендинг) — допустима, только релевантный контент, отключена для premium-подписчиков, не раньше Phase 4-5. См. D44 для полного rationale.
- ❌ **Hard paywall** — отрезает виральность
- ❌ **Lifetime deal** — на старте может казаться спасением, но запирает revenue на 0 через 6 месяцев

---

## 14. Backlog (каждый item: что / зачем / в перспективе)

### High priority post-launch

#### A1. Share card (вирусный механизм)

- **Что:** Красивая обезличенная карточка дерева для шеринга в соцсети. Силуэт дерева + ветки фокуса (без интимных деталей корней/ресурсов) + вотермарка rootsnfroots.com.
- **Зачем:** Главный driver виральности и органики. Без него — придётся платить за каждого пользователя.
- **В перспективе:** Премиум-апселл "убрать вотермарку". Sezonny shareable artifacts (как Spotify Wrapped) — "Моё дерево 2027".

#### A2. Multi-tree (несколько деревьев)

- **Что:** Создать новое дерево, сохранив корни и ствол, но с другим горизонтом или фокусом.
- **Зачем:** Поддерживает use-case "1 год + 3 года параллельно" или "личное + рабочее". Один из главных запросов с интервью (гипотеза).
- **В перспективе:** Основная премиум-фича. Также — entry point для будущих типов деревьев (см. C1).

#### A3. History / Timeline view

- **Что:** Visual timeline изменений дерева. Что было месяц назад, как менялось.
- **Зачем:** Сильнейший retention-механизм + смысл возвращения. Превращает дерево в живую запись жизни.
- **В перспективе:** Премиум-фича. Создаёт effect "история со мной" → эмоциональная привязка → snijдает churn.

#### A4. Smart reminders

- **Что:** Уведомления о возвращении на основе паттернов (push если PWA или native, email иначе). Customizable частота.
- **Зачем:** Прямо в нашу North Star (WAR). Большинство wellness-юзеров **не возвращаются если их не подтолкнуть**.
- **В перспективе:** Премиум. Можно делать AI-нативные ("Прошёл месяц с тех пор как ты добавил «Здоровье» в ветки. Появились ли плоды на этой ветке?").

### Medium priority

#### B1. Advanced questions & somatic practices (контент Вячеслава)

- **Что:** Расширенный пул вопросов в drawer + соматические упражнения (5-10 мин) для каждой зоны.
- **Зачем:** Углубляет ценность ровно тем, что Вячеслав знает лучше всего как коуч. Уникальный апселл, который нельзя скопировать.
- **В перспективе:** Премиум-content. Можно превратить в курс ("Курс по самоопоре" на 30 дней).

#### B2. PDF export (красивый)

- **Что:** Дерево как PDF — для печати или личного архива.
- **Зачем:** Эмоциональный артефакт ("моё дерево на 2027 год — распечатать и повесить"). Премиум-апселл.
- **В перспективе:** Может стать collectable item, marketing channel (распечатанное и подаренное другу = виральность).

#### B3. Mobile UX / PWA / native apps

- **Что:** Полноценная мобильная версия. Сначала PWA, потом возможно native через React Native / Tauri.
- **Зачем:** Большая часть аудитории на телефонах. Без этого — теряем 70% потенциальных юзеров.
- **В перспективе:** Native apps дают доступ к App Store / Play Market — массовая дистрибуция.

#### B4. English version

- **Что:** Полная локализация на английский.
- **Зачем:** Доступ к глобальному рынку (×100 размер аудитории).
- **В перспективе:** Возможно — другие языки (немецкий, испанский, португальский).

### Lower priority / experimental

#### C1. Tree templates / archetypes

- **Что:** Готовые шаблоны деревьев (например, "Дерево для смены карьеры", "Дерево восстановления после кризиса"). Не заполняют за юзера, а дают **отправную структуру**.
- **Зачем:** Снижает порог входа для тех, кто не знает с чего начать.
- **В перспективе:** Контентный пайплайн (могут создавать другие коучи под roof of platform).

#### C2. Collaborative trees (опционально для пар/семей)

- **Что:** Совместное дерево пары или семьи — общие цели, общие ресурсы.
- **Зачем:** Новый use-case, новая аудитория.
- **В перспективе:** Family plan премиум.

#### C3. AI insights / reflection assistant

- **Что:** Опциональный AI-помощник, который видит дерево и задаёт уточняющие вопросы ("Я вижу у тебя в стволе 'выносливость' — расскажи в каком контексте она была опорой?").
- **Зачем:** Углубляет рефлексию для тех, кто хочет, без замены человеческого коуча.
- **В перспективе:** Premium-фича. **Осторожно с reliability** — AI не должен давать advice, только задавать вопросы.

#### C4. Coach marketplace

- **Что:** Платформа коучей, использующих метод дерева в своей практике.
- **Зачем:** Масштабирование без участия Вячеслава лично. Сетевой эффект.
- **В перспективе:** Долгосрочно — переход от продукта к платформе. Только если основной продукт стабильно работает.

#### C5. Print-on-demand integration

- **Что:** Заказать своё дерево как принт / постер / canvas.
- **Зачем:** Эмоциональный артефакт, новый revenue stream.

#### C6. Tree as voice assistant input

- **Что:** Голосовое заполнение через диктовку.
- **Зачем:** Снижает порог входа для тех, кому проще говорить чем писать.

---

## 15. Research Base

### Размер рынка

- Mental wellness apps: **$6-9B в 2024-2025**, CAGR ~15-17%, прогноз $22-45B к 2033-2035 (разные источники сходятся на двузначном росте)
- Subsegment "wellness management" растёт быстрее всех — ~17.91% CAGR
- Self-development / introspection — узкая ниша внутри, недо-обслуженная сегодня

### Competitive landscape

| Игрок | Фокус | Где мы отличаемся |
|------|-------|------------------|
| Calm | Расслабление, медитации | Мы про самопознание и цели, не про "успокоиться" |
| Headspace | Mindfulness + sleep | Аналогично — другая боль |
| BetterHelp / Talkspace | Терапия с живым человеком | Мы — практика без человека (но с авторским методом) |
| Stoic, Reflectly | Daily journaling | Мы — структурная карта, а не daily writing |
| Finch | Care + cute pet | Мы — взрослый рефлексивный инструмент |
| 16personalities, MBTI | Personality test | Мы — не тип, а контекст моей жизни |
| Best Self Journal | Goal-setting структурой | Мы цифровые + связываем цели с опорами/ресурсами (они не делают) |

**Наша позиция:** в пересечении "self-knowledge" + "goal-setting" + "live document" — никто не закрывает все три одновременно.

### Психологическая база (см. About page для full text)

1. **ACT** — Hayes, Harris. Meta-analysis Hedges' g = 0.72 на тревогу/депрессию (Springer 2025).
2. **Implementation Intentions** — Gollwitzer & Sheeran. d = 0.65, meta of 94 studies.
3. **Self-compassion** — Kristin Neff. Meta of 56 RCTs (Mindfulness, 2023).
4. **Interoception** — Damasio, Garfinkel. Systematic review (Frontiers, 2020).
5. **Polyvagal theory practice** — Deb Dana, *Anchored: How to Befriend Your Nervous System*.

### Почему мы верим в монетизацию

- Wellness sector — один из самых монетизируемых в B2C SaaS
- Conversion rate freemium → premium в нише: **2.18% median** (Lenny's Newsletter / RevenueCat data)
- ARPPU в нише: $30-60/год
- При WAR > 25% и 1000 активных юзеров: ~$600-1200/мес revenue at minimal scale, scaling to ~$50-300K/year by year 2-3 with organic growth + light paid marketing
- **Защита от конкурентов:** уникальный метод + личный бренд автора → defensible, нельзя скопировать "просто скопировав фичи"

### Почему людям полезно (психологические "точки удара")

1. **Externalization of self.** Видеть себя со стороны, не варясь внутри. Психологически doku мощно (gestalt, schema therapy).
2. **Reduces cognitive load.** Удерживать в голове 6 опор + 5 ресурсов + 5 веток + 5 плодов одновременно невозможно. Дерево их *удерживает за тебя*.
3. **Connects past–present–future.** Корни (прошлое) ↔ Ствол (настоящее) ↔ Крона (будущее). Большинство приложений работают только с одной из этих временных плоскостей.
4. **Embodied reflection.** Вопросы про тело — редкость в digital-продуктах. Это даёт unique experience и реально работает (по интероцепции исследования).
5. **No pressure.** Не daily-streak, не "проиграл если пропустил". Уважение к темпу = меньше churn по моральным причинам.

### За счёт чего возьмём долю рынка

1. **Уникальный метод "самоопора"** — нет аналогов в текущих продуктах
2. **Личный бренд Вячеслава** — autoritet и trust из коуч-практики
3. **Виральность через share card** — экспоненциальный органик-рост возможен
4. **Defensible content** — vопросы, примеры, examples — авторский, не GPT-generated
5. **Нет конкурентов в точной нише** — пересечение трёх категорий уникально

---

## 16. Decision Log (Development Legends)

История архитектурных и продуктовых решений с rationale. Обновляется при каждом значимом решении.

### 2026-Q2 prototype phase

**D1. Использовали single-file HTML+React+Babel в браузере для прототипа.**
- Rationale: Скорость итерации. Минимум overhead для валидации идеи.
- Trade-off: Хрупкость к изменениям CDN-зависимостей (см. L1 в Lessons).
- В Production: уходим в Next.js.

**D2. Координаты слотов жёстко закодированы в процентах.**
- Rationale: Картинка дерева фиксированная. Проценты дают resolution-independence.
- Альтернатива: SVG-карта с координатами — overkill для текущего scope.
- Менять при: смене картинки дерева.

**D3. Лимит 5 веток фокуса — жёсткий.**
- Rationale: Психологическая нагрузка, теория ограниченного внимания (Miller, 7±2). Больше веток = расфокус. Это product principle.
- При жалобах: НЕ увеличивать механически. Сначала понять что **на самом деле** хотят — возможно нужны multi-tree.

**D4. Лимит 6 корней — мягкий (можно перетаскивать).**
- Rationale: 6 домиков на картинке. Перетаскиванием можно нарушить, и это ок — корни не должны давить лимитом.
- Поведение: первые 6 идут в слоты, дальше — спавн возле земли.

**D5. Drag самописный, не библиотека.**
- Rationale: В прототипе нужен был полный контроль и простота.
- В Production: переходим на `@dnd-kit/core` — touch-friendly из коробки.

**D6. Welcome-модалка показывается ОДИН раз (флаг в localStorage).**
- Rationale: Не давить. Юзер прочитал — больше не нужно.
- Возврат: через меню `⋯` → "Снова показать введение".

**D7. Horizon dialog появляется ПЕРЕД первым плодом, не на старте.**
- Rationale: На старте юзер не знает зачем выбирать. Появление в контексте (когда уже что-то добавил, дошёл до плодов) даёт мотивацию ответить осмысленно.

**D8. Все элементы кроме веток — draggable.**
- Rationale: Свобода позиционирования = ownership. Branches фиксированы потому что они отражают **структуру дерева** (5 веток в кроне), не контент.

**D9. Tree image на фоне через `background-image: cover` + blur, главное изображение — `<img>` с object-fit container.**
- Rationale: Контент сцены не обрезается на разных экранах, фон закрывает "пустые" сине-серые поля по бокам.
- Альтернатива (отвергнута): растянуть `tree.png` через cover на основной → теряем точность позиционирования.

**D10. Цветовая палитра — приглушённые землистые тона.**
- Rationale: Wellness-контекст. Яркие crayola-цвета (initial version) выглядели как "детский продукт", не как взрослый инструмент рефлексии. См. L2.

**D11. Шрифты Manrope (UI) + Fraunces (serif для бренда и заголовков).**
- Rationale: Pair используется в современных wellness/coaching-продуктах (близкие vibes Oura, Calm). Manrope — тёплый geometric sans, читаемый. Fraunces — editorial serif, добавляет авторитет без формальности.

**D12. Деревьев пока одно на юзера, multi-tree — premium-фича.**
- Rationale: Упрощает onboarding (нет выбора "какое дерево создаём"). Multi-tree приходит когда юзер уже привязался.

**D13. "Сорванный плод" визуально = надкус (CSS-маска) + зачёркивание + цвет приглушённый + галочка.**
- Rationale: Метафора прямая (сорвал — съел). Дофамин маленький, но осязаемый. См. также раздел про harvested_count как teaser виральности ("я сорвал N плодов").
- Реализация надкуса через `mask-image: radial-gradient` — надёжно работает во всех современных браузерах. Старая попытка через `clip-path: path()` была хрупкой.

**D14. North Star = Weekly Active Returners.**
- Rationale: см. раздел 10. Совместима с продуктом (не daily), читается как PMF-сигнал, защищает от dark patterns.

**D15. Аналитика — PostHog.**
- Rationale: см. сравнение в Tech Stack. Один инструмент для events + replays + flags + funnels.

**D16. Free-only at launch, premium через 2-4 месяца.**
- Rationale: см. раздел 13. Предотвращает преждевременную монетизацию и потерю виральности.

**D17. От автора — Вячеслав, "коуч по самоопоре".**
- Rationale: Уникальное позиционирование, прямая связь с метафорой продукта, отличает от широкой массы коучей. Trade-off: новое слово для холодного юзера, но в конце "О подходе" уже принято.

### 2026-07-17 Phase 0 → Phase 1 transition

**D18. Next.js 16 + Tailwind CSS v4 вместо заявленных "14+".**
- Rationale: На момент scaffolding `create-next-app@latest` ставит 16.2.10 / Tailwind v4 — новее, чем версия, названная в разделе 9, но удовлетворяет "14+". App Router, `next/font`, Server Components — всё как задумано.
- Заметка: Next 16 несёт breaking changes относительно того, что могло быть в обучающих данных модели — при генерации кода сверяться с `node_modules/next/dist/docs/` (см. авто-сгенерированный `AGENTS.md` в корне репо).

**D19. GitHub push через SSH-ключ, не через `gh` CLI.**
- Rationale: На машине фаундера нет Homebrew, `gh` CLI недоступен без него. Вместо установки Homebrew ради одного инструмента — сгенерирован новый ed25519 SSH-ключ, добавлен фаундером в GitHub-аккаунт вручную. Репозиторий `SnowVB/rootsnfroots` фаундер создал сам через веб-интерфейс.
- Trade-off: без `gh` CLI создание issues/PR из терминала потребует либо веб-интерфейса, либо установки `gh` позже (например через прямой бинарник с GitHub Releases, без sudo).

**D20. Vercel и Supabase подключаются через дашборд, не CLI.**
- Rationale: Оба сервиса требуют OAuth-логина фаундера в браузере — Claude Code не может пройти этот флоу от его имени. Vercel: `vercel.com/new` → import `SnowVB/rootsnfroots`. Supabase: проект создаётся в дашборде, ключи (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`) передаются в `.env.local` (не коммитится, см. `.env.local.example`).
- Реализовано заранее: `@supabase/ssr` + `@supabase/supabase-js` установлены, клиенты-хелперы в `src/lib/supabase/client.ts` (browser) и `src/lib/supabase/server.ts` (server, cookie-based session). Auth-flow и схема БД — отдельный шаг Phase 1, ждут реального Supabase-проекта.

**D21. `vercel link --yes` без явного `--project` создаёт новый проект, а не линкует существующий.**
- Rationale: Наступили на эту грабли сами — `vercel link --yes` в первый раз тихо создал пустой дубликат-проект `tree-of-support` вместо линковки к уже задеплоенному `rootsnfroots`, и подключил к нему тот же GitHub-репозиторий (риск двойного деплоя на каждый push). Удалили дубликат, перелинковали через `vercel link --project rootsnfroots --yes`.
- На будущее: при линковке существующего Vercel-проекта всегда указывать `--project <name>` явно, не полагаться на автоопределение по имени директории.

**D22. Схема БД — SQL-миграция в репо + ручной прогон через Supabase SQL Editor, не `supabase db push`.**
- Rationale: `supabase link`/`db push` требуют либо пароль от Postgres (Settings → Database), либо `supabase login`, который сам требует personal access token уровня аккаунта (шире по правам, чем anon key) — CLI отказывается от device-auth флоу вне TTY. Ради одного первого прогона схемы это лишнее трение и лишний секрет для хранения.
- Компромисс: `supabase/migrations/0001_init.sql` — источник правды в git, но применяется вручную через SQL Editor. TypeScript-типы (`src/lib/supabase/database.types.ts`) написаны руками по той же миграции вместо `supabase gen types` — по той же причине (нет access token). При изменении схемы — обновлять миграцию и типы в одном коммите.
- Пересмотреть при: частых изменениях схемы (тогда трение CLI-флоу с одноразовым получением access token окупится) или при найме второго разработчика (нужен воспроизводимый non-interactive способ применять миграции, а не «зайди в дашборд и вставь SQL»).

**D23. Component library стартовал раньше Auth, на локальном (Zustand + localStorage) стейте, не на Supabase.**
- Rationale: RLS в `0001_init.sql` требует `auth.uid() = user_id` — писать в Supabase без auth физически нельзя. Roadmap (§17) уже ставит "Component library" раньше "Auth", а модель хранения (§4, D12) описывает anonymous-first поток: локальная сессия → миграция в БД при signup. Так что Zustand-стор с `localStorage`-персистом — это не заглушка, а первая реализация именно этого anonymous-слоя, а не одноразовый mock на выброс.
- Реализовано: `src/store/useTreeStore.ts` (persist middleware, ключ `tos-tree-items-v1`, `skipHydration: true` + `onRehydrateStorage` — иначе SSR/CSR hydration mismatch, т.к. на сервере localStorage не существует). Компоненты: `RootItem`, `TrunkItemView`, `BranchItem`, `FruitItem` (+ `DeleteBtn`, `FruitMenuItem`, `useDraggable`) в `src/components/tree/`, портированы из `prototype/index.html` на Tailwind classes + inline style только для вычисляемых значений (позиция, drag-состояние).
- Что специально НЕ перенесено в этот проход (чтобы не оставлять недоделанные UI-привязки — см. product principle "no half-finished implementations"): `InfoPopover`, `ExampleModal`, `WelcomeModal`, `HorizonDialog`, `QuestionsDrawer`, `AboutPage`, кнопка сворачивания ⋯-меню в правой панели (menu items оттуда зависят от About/Horizon/export, которых ещё нет). Из-за отсутствия `InfoPopover` кнопки добавления временно без иконки `i`; текст подсказки в правой панели (§7.8) на время адаптирован ("Кликни на элемент, чтобы отредактировать") — **вернуть точный текст §7.8 после того как `InfoPopover` будет собран**, это осознанное временное расхождение с текстом источника, не финальное решение.
- Drag остался на самописном `useDraggable` (1:1 порт прототипной логики с порогом 3px), не на `@dnd-kit` — это отдельный пункт roadmap ("Drag & Drop: @dnd-kit integration"), сознательно не смешан с этим проходом.
- Побочная находка: у Fraunces на Google Fonts нет кириллического сабсета (`next/font`'s `font-data.json` подтверждает — только `latin`, `latin-ext`, `vietnamese`). Кириллические заголовки на `font-serif` будут рендериться в fallback (`Georgia, serif`), не в Fraunces. На дизайн это может слегка повлиять — заголовки на кириллице не получат тот же графический характер, что латинские акценты. → Решение см. D24.

**D24. Serif-бренд-шрифт заменён с Fraunces на Literata (везде, не только для ru).**
- Rationale: продукт russian-first (§1) — почти все заголовки на кириллице, значит отсутствие кириллицы у Fraunces (см. D23) это не мелкий нюанс, а реальный пробел в бренде на старте. Сверили через `next/font`'s `font-data.json` (тот же источник, на котором держится весь font-loading) список серифов с реальной кириллицей: `Playfair Display`, `Lora`, `PT Serif`, `Bitter`, `Cormorant`, `Cormorant Garamond`, `Noto Serif`, `Alegreya`, `Spectral`, `Literata`, `Yeseva One`, `Philosopher`, `Ysabeau` — все технически валидны. Literata выбрана фаундером как ближайшая по характеру к Fraunces (тёплый humanist book-serif от Google, курсив есть, веса 300-900) среди вариантов с кириллицей.
- Один шрифт на обе локали (ru и en), не разный по языку — иначе бренд визуально расходится между рынками в зависимости от того, на каком языке скриншот.
- Реализовано: `src/app/layout.tsx` (`Literata` вместо `Fraunces`, `subsets: ["latin","cyrillic"]`), `src/app/globals.css` (`--font-serif: var(--font-literata), Georgia, serif`).
- Обновить при: если позже захотим более "дизайнерский"/контрастный акцентный шрифт для маркетинговых материалов (лендинг, шеринг-карточка) — там ограничение по кириллице то же самое, держать в уме при выборе.

**D25. Один домен (`rootsnfroots.com`) на ru и en, не два отдельных сайта/домена. Русский — корень без префикса сейчас, `/en/` добавляется позже. Auto-detect по `Accept-Language` + явный переключатель в UI.**
- Контекст: всплыл вопрос при обсуждении D24 (менять ли serif-шрифт "только для ru" или везде) — оказалось, что нигде в проекте не было явно зафиксировано, один сайт или два, и какая локаль сидит на корне. В коде/roadmap (§9 "i18n (later)": `next-intl`, §17 "Домен `rootsnfroots.com` направлен на Vercel") всегда подразумевался один домен, но явного решения записано не было — фаундер подтвердил и уточнил детали.
- Rationale за один домен: концентрация SEO-авторитета вместо раскола между двумя; `rootsnfroots.com` уже куплен именно под это; один сайт с языковыми путями легко переиграть редиректом позже — обратное (сращивать два независимых домена) намного дороже.
- **Схема на старте:** `rootsnfroots.com` (корень, без префикса) = русский — это единственный рынок запуска (§1, §2.7). `/en/` появляется как путь, когда стартует английская локализация (Backlog B4) — не обязательно через 6-12 месяцев, может раньше, параллельно с ru.
- **Будущий swap (зафиксирован, но НЕ закладывается в архитектуру сейчас):** когда en-версия будет готова к полноценному использованию, домен переворачивается одномоментно (не поэтапно) через 301-редиректы — английский переезжает на корень, русский переезжает на `/ru/`. Все внешние ссылки (соцсети, Telegram-канал, шеринг-карточки и т.п.) обновляются вручную в момент свопа, не заранее. До этого момента ничего в коде под этот swap не резервируется — типичный пример "не проектировать под гипотетические будущие требования раньше времени".
- Из-за этого D24 (Literata вместо Fraunces) применяется на оба языка без исключений — раскол бренда по локали был бы такой же ошибкой, как раскол домена.
- Реализация роутинга (`next-intl`, `/[locale]/` сегмент) откладывается до появления реального английского контента (Backlog B4). Когда B4 стартует — рефактор `app/page.tsx` → `app/[locale]/page.tsx` per next-intl docs с русским как default locale (без префикса) — стандартный, некритичный шаг на тот момент.
- Пересмотреть при: если на этапе локализации выяснится, что нужен принципиально разный копирайт/оффер под en-рынок (не просто перевод) — тогда одного домена с языковыми путями может быть недостаточно, и стоит вернуться к вопросу отдельного домена осознанно, а не по умолчанию.

**D26. Auth: и email magic link, и Google OAuth, оба сразу — не последовательно.**
- Rationale: фаундер подтвердил готовность сразу настроить оба способа входа, разницы в сложности после первоначальной настройки нет. Magic link не требует внешних сервисов (Supabase справляется сам), Google OAuth требует один раз пройти Google Cloud Console — фаундер это сделает.
- Реализовано: `middleware.ts` + `src/lib/supabase/middleware.ts` (session refresh, стандартный `@supabase/ssr` паттерн для Next.js), `src/app/auth/page.tsx` (форма email + кнопка Google, Server Actions, без клиентского JS для самой формы), `src/app/auth/actions.ts` (`signInWithEmail`, `signInWithGoogle`, `signOut`), `src/app/auth/callback/route.ts` (обмен `code` на сессию через `exchangeCodeForSession`, только для Google OAuth — см. D27 про magic link). `src/app/page.tsx` стал Server Component (читает пользователя через `supabase.auth.getUser()`, передаёт `userEmail` в клиентский `TreeApp`), реальный UI дерева переехал в `src/components/tree/TreeApp.tsx`.
- Google OAuth: фаундер создал OAuth Client ID в Google Cloud Console и вставил Client ID + Secret в Supabase Dashboard → Authentication → Providers → Google — подтверждено рабочим (`GET /auth/v1/settings` → `"google": true`, `/auth/v1/authorize?provider=google` реально редиректит на `accounts.google.com` с валидным `client_id`).
- Что специально НЕ сделано в этот проход: миграция анонимной localStorage-сессии в Supabase при первом входе — это отдельный пункт roadmap ("Анонимная сессия → миграция в БД при signup"). После входа дерево пока продолжает читаться из localStorage независимо от auth-статуса — так и должно быть, пока миграция не собрана; см. также D23.
- Legal/GDPR: явно НЕ входит в этот проход (см. §17 Phase 2, не начато). Google OAuth означает передачу данных третьей стороне — это делает Privacy Policy более срочной задачей к моменту публичного запуска, но writing юридических текстов не блокирует технический Auth-код.

**D27. Email magic link верифицируется через `token_hash` + явное подтверждение кликом, не через `exchangeCodeForSession` в `/auth/callback`.**
- Контекст: первый живой тест magic link (фаундер) провалился — `/auth/callback?code=...` пришёл с валидным кодом от Supabase, но `exchangeCodeForSession` всё равно вернул ошибку; повторные попытки по той же ссылке падали с `otp_expired` от самого Supabase (лог: `error=access_denied&error_code=otp_expired`). Причина — PKCE-флоу `exchangeCodeForSession` требует cookie `code_verifier`, сохранённый в момент запроса ссылки, всё ещё присутствующий в браузере в момент клика по ссылке из письма; эта cross-request continuity хрупкая (и дополнительно на неё могут влиять автоматические prefetch-боты почтовых клиентов типа Gmail/Outlook Safe Links, которые "кликают" по ссылке раньше человека и сжигают одноразовый токен).
- Решение: `supabase.auth.verifyOtp({ token_hash, type })` — отдельный метод верификации, не завязанный на cookie-continuity вообще. Ссылка в письме ведёт не на хостed Supabase `/auth/v1/verify`, а сразу на наш `/auth/confirm?token_hash=...&type=email` (шаблон письма переопределён в Supabase Dashboard → Authentication → Email Templates → Magic Link: `{{ .ConfirmationURL }}` → `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email`). `/auth/confirm` — Server Component, который НЕ верифицирует токен на GET (это защищает от prefetch-ботов, которые делают только GET и не жмут кнопки) — верификация происходит только по явному клику на кнопку "Подтвердить вход" (Server Action `confirmMagicLink` в `src/app/auth/actions.ts`).
- `/auth/callback` (`exchangeCodeForSession`) остался — но теперь только для Google OAuth, где cookie-continuity не проблема (один и тот же браузер/вкладка на всём пути через Google, без email и без второго устройства).
- Пересмотреть при: если понадобится "вход по коду" (6 цифр, вручную вбить) вместо ссылки — тот же `verifyOtp` подходит, просто `token` вместо `token_hash`, форма вместо auto-filled hidden fields.
- Подтверждено рабочим живым тестом фаундера после подключения custom SMTP (см. D28): письмо доходит, кнопка "Подтвердить вход" логинит успешно. Письмо пока попадает в спам — ожидаемо для домена без истории отправок, не блокер, см. §19 п.6.

**D28. Custom SMTP (Resend) подключен раньше срока — не в Phase 4, а сразу, чтобы разблокировать magic link.**
- Контекст: Supabase блокирует редактирование email-шаблонов (Subject/Body) на дефолтном мейлере на **любом** плане, бесплатном или платном — это анти-спам ограничение платформы, не апселл. Без кастомного SMTP невозможно было применить фикс из D27 (шаблон должен вести на `/auth/confirm`, а не на дефолтный `{{ .ConfirmationURL }}`). Resend уже был выбран в §9 Tech Stack ("Email (later)") — просто выполнили раньше плана, не новая архитектурная сущность.
- Реализовано: домен `rootsnfroots.com` верифицирован в Resend (DKIM + SPF TXT-записи и MX-запись под "Custom MX" в Namecheap Advanced DNS — DNS домена управляется в Namecheap, не в Vercel/Cloudflare, см. `dig NS rootsnfroots.com` → `dns1/2.registrar-servers.com`). Supabase Dashboard → Authentication → Emails → SMTP Settings: host `smtp.resend.com`, port 465, username `resend`, password — Resend API key, sender `noreply@rootsnfroots.com` / "Дерево Опоры".
- §9 таблица обновлена — Resend больше не "(later)", уже в проде.
- Пересмотреть при: если объём писем вырастет настолько, что понадобится отдельный dedicated IP или продвинутый DMARC (`p=reject`) — сейчас достаточно дефолтного `p=none` (monitor-only).

**D29. Анонимная сессия → миграция в Supabase при первом входе, реализовано сразу после Auth (не отложено).**
- Rationale: после D26 signing in делал ровно ничего функционально — дерево оставалось только в localStorage независимо от auth-статуса, то есть `/auth`'ная фраза "чтобы дерево сохранялось и было доступно с других устройств" была неправдой. Это разрыв между тем что Auth обещает и что реально происходит — logically следующий шаг, не отдельная фича на потом.
- Реализовано: `src/lib/supabase/tree.ts` — data-access слой (browser client, значит RLS `auth.uid()` применяется автоматически): `findTreeId`/`createTreeId`, `fetchTreeItems`, `migrateLocalItemsToSupabase`, по-zone `insert*`, общие `updateItemTextRemote`/`deleteItemRemote`/`dragItemRemote`/`toggleHarvestRemote`. `useTreeStore` получил `userId`/`treeId`/`remoteLoading` + `initForUser`/`clearUser`; каждый мутатор (add/update/delete/drag/harvest) теперь дополнительно (не вместо) пишет в Supabase, когда юзер залогинен — локальный `set()` остаётся источником мгновенного UI-отклика, remote-запись летит следом (fire-and-forget, ошибки в `console.error`, без retry/rollback — сознательно просто для этого прохода).
- **Миграция:** при первом входе, если у юзера ещё нет `trees`-строки — создаём её и bulk-insert'им текущие локальные items (id при инсерте передаём тот же, что уже сгенерирован локально через `crypto.randomUUID()` — не нужно потом сверять/переприсваивать id). При повторном входе (tree уже существует) — просто грузим items из Supabase, локальные (если есть) отбрасываются.
- **Приватность (CLAUDE.md принцип #1):** пока юзер залогинен, `persist`'s `partialize` пишет в localStorage `EMPTY_ITEMS`, а не реальные items — иначе дерево одного аккаунта осело бы в анонимном localStorage-слоте и утекло бы в следующую анонимную сессию на том же браузере. При sign-out (`clearUser`) — items сбрасываются в `EMPTY_ITEMS`, не пытаемся восстановить до-логиновый анонимный снэпшот (тот уже мигрирован в аккаунт, хранить второй "теневой" экземпляр избыточно и путает).
- **Race condition:** `initForUser` читает текущий `items` для решения "есть ли что мигрировать" — вызывается только после того как local rehydration (`hasHydrated`) уже завершился (см. `TreeApp.tsx` — эффект явно ждёт `hasHydrated` перед вызовом), иначе можно ошибочно решить "мигрировать нечего", пока localStorage ещё не прочитан в память.
- **Побочная находка (важно на будущее):** руками написанный `database.types.ts` перестал типиться на текущей версии `@supabase/supabase-js`/`postgrest-js` (2.110.7) — без полей `Relationships` (на каждой таблице) и `Views`/`Functions`/`Enums`/`CompositeTypes` (на схеме) весь `Database`-тип не проходит `GenericSchema`-constraint и все таблицы схлопываются в `never`. Раньше (на момент D22) этого не требовалось — значит между тем и этим моментом postgrest-js обновил свой type-engine. **Если снова увидите `never`/`does not exist on type never` при работе с Supabase-типами — первым делом проверяйте эти поля в `database.types.ts`, не сам запрос.**
- Что НЕ сделано: `Очистить дерево` (menu item) не трогает Supabase (сам menu item ещё не собран, см. D23); realtime-синхронизация между вкладками/устройствами не реализована (загрузка происходит один раз при входе, не подписка).
- Пересмотреть при: если появится второй клиент того же аккаунта одновременно (два открытых окна) — сейчас нет write-conflict защиты (last-write-wins по факту), некритично пока один юзер = одна активная сессия.

**D30. WelcomeModal, InfoPopover, ExampleModal собраны — следующий логичный шаг после D23 (не Auth-связанные, чисто первый-визит опыт).**
- Rationale: без них CJM (§3) обрывается на "видит пустое дерево" — ни объяснения метафоры (welcome), ни определений зон (`i`-иконка), ни примеров. Это ядро продуктовой дифференциации (§1, §15 — "self-knowledge как отдельная категория"), а не косметика; выбрано раньше PostHog/feature flags/@dnd-kit, потому что измерять и флагировать пока особо нечего без завершённого core-опыта.
- Реализовано: `src/lib/tree/copy.ts` — весь текст (`ZONE_DEFINITIONS`, `ZONE_EXAMPLES`, `WELCOME_TEXT`) как данные, 1:1 с CLAUDE.md §7.2–7.4, отдельно от `constants.ts` (структурные данные слотов). `WelcomeModal`/`ExampleModal` рендерятся в `TreeApp.tsx` на верхнем уровне (не внутри `RightPanel` — `ExampleModal` полноэкранный, должен перекрывать и сцену дерева, и панель, как в прототипе). `AddButton` получил обратно иконку `i` (была временно убрана в D23, пока `InfoPopover` не существовал) — `popoverZone`-стейт живёт в `RightPanel`, `exampleZone`-стейт лифтнут в `TreeApp` именно из-за полноэкранности модалки. Текст подсказки в правой панели (§7.8) возвращён к каноническому виду — временное расхождение из D23 закрыто.
- **Гидратация без setState-в-эффекте:** для флага `tree_intro_seen_v1` использован `useSyncExternalStore` (server snapshot всегда `true` = "уже видел", реальное значение читается из localStorage только на клиенте) вместо `useState` + `useEffect`. Это не стилистический выбор — новый ESLint-правило `react-hooks/set-state-in-effect` (часть `eslint-plugin-react-hooks`) блокирует именно паттерн "прочитать localStorage в эффекте и вызвать setState" как error, не warning. `useSyncExternalStore` — официально рекомендованный React-хук ровно для этого класса задач (клиент-only значение, которое не должно вызывать hydration mismatch), решает и лишний render, и сам факт наличия эффекта. **Общий паттерн на будущее:** для любого "прочитать что-то из browser-only API один раз при маунте" — предпочитать `useSyncExternalStore`, не `useEffect` + `useState`.
- Что НЕ входит: повторный показ welcome через `⋯`-меню (меню ещё не собрано, см. D23) — сейчас welcome показывается только автоматически при первом визите.

**D31. HorizonDialog собран и подключен к первому добавлению плода (CJM §3, шаг 7) — раньше это молча пропускалось.**
- Контекст: до этого прохода `addFruit` в сторе не проверял горизонт вообще — плоды добавлялись без единого вопроса о сроке, хотя CJM и §7.6 явно описывают этот момент как часть первого опыта. Реальный функциональный пробел, не просто недостающий компонент.
- Реализовано: `horizon: Horizon | null` в `useTreeStore` (тот же `persist`, та же приватность что и `items` — `partialize` прячет horizon в localStorage пока юзер залогинен, `clearUser` сбрасывает в `null` при выходе). Для Supabase — `trees.horizon` уже была в исходной миграции (0001_init.sql), просто не читалась/не писалась до сих пор: `findTree`/`createTree`/`updateTreeHorizonRemote` в `src/lib/supabase/tree.ts`. При первой миграции анонимного дерева в аккаунт (D29) локальный horizon (если был выбран анонимно) переносится в новую `trees`-строку тоже.
- `TreeApp.openAdd('crown')` теперь проверяет `items.crown.length === 0 && !horizon` перед открытием формы плода — если да, сначала `HorizonDialog`, и только после выбора (не после "Пропустить") открывается форма плода. Смена горизонта позже — через pill в шапке `RightPanel` (виден только когда `horizon` уже задан).
- Текст — 1:1 с §7.6, вынесен в `src/lib/tree/copy.ts` (`HORIZON_TEXT`, `HORIZON_OPTIONS`, `HORIZON_LABELS`).
- Отличие от прототипа: там horizon хранился в отдельном localStorage-ключе `tree_horizon_v1`, здесь — в том же Zustand-сторе что и `items`, ради консистентности privacy/hydration-логики (не дублировать "подождать гидратации перед миграцией" на второй независимый источник данных).

**D32. QuestionsDrawer собран — последний крупный кусок component library (CJM §3, шаг 8) кроме AboutPage.**
- Rationale: по исследовательской базе (§15) — вопросы с телесным откликом ("Тело подсказывает") являются уникальным отличием метода, не воспроизводимым конкурентами. Без этого компонента продукт был просто структурированной формой ввода, без единственной вещи, которая реально отличает его от чек-листов/трекеров целей.
- Реализовано: `src/lib/tree/copy.ts` — `ZONE_QUESTIONS` (все 4 зоны × 3 уровня, 1:1 с §7.5), `QUESTION_LEVEL_INFO`. `QuestionsDrawer` рендерится внутри `TreeScene` (не на уровне `TreeApp`) — в отличие от полноэкранных модалок, шторка позиционирована `absolute` строго в пределах сцены дерева (левая часть), не должна перекрывать правую панель; т.к. её собственный backdrop — плоский `bg-black/15` без `backdrop-filter`, проблема из L13 (наложенные blur-слои) сюда не применяется, портал не понадобился.
- `openSection` (какая из 3 глубин раскрыта) сбрасывается на `'start'` при смене таба зоны — сделано через "adjust state during render" (сравнение `activeZone` с `prevZone`, оба в state, setState прямо в теле компонента при расхождении), а не `useEffect` + `setState` — тот же `react-hooks/set-state-in-effect` lint-error, что и в D30/D31, официально рекомендованный React-паттерн именно для "сбросить state при изменении другого state/пропа".
- Стейт `drawerOpen` — локальный в `TreeScene`, не поднят в `TreeApp`: шторка не нужна больше нигде, незачем поднимать состояние выше того уровня, где оно реально используется.
- Что НЕ входит: закрытие по Escape (клик по backdrop есть, клавиатурного шортката нет — не критично, добавить позже если понадобится accessibility-проход).

**D33. AboutPage реализован как настоящий роут `/about` (Server Component), не как модалка поверх дерева — закрывает сразу два пункта roadmap (Component library + Routing).**
- Rationale: §17 явно разделял "Component library" и "Routing: `/` (tree), `/about` (как страница)" на два отдельных пункта — то есть страница "О подходе" в проде всегда подразумевалась настоящим URL, не оверлеем как в прототипе (`AboutPage({ onClose })`). Реальный роут даёт: собственный URL для шеринга (человек может прислать ссылку на `/about` отдельно от дерева), SEO (собственные `<title>`/`description` через `export const metadata`), и не требует клиентского JS вообще — весь контент статический.
- Реализовано: `src/app/about/page.tsx` — Server Component (без `"use client"`, нет интерактивности кроме обычных `<Link>` переходов), билд подтверждает `/about` как `○ (Static)` — полностью prerendered. Весь текст — `ABOUT_TEXT` в `src/lib/tree/copy.ts`, 1:1 с §7.9. Ссылка "О подходе →" добавлена в футер `RightPanel` (вместе с `AuthStatus`, над ним).
- Что НЕ входит: `⋯`-меню в правой панели по-прежнему не собрано (см. D23) — там тоже должен быть пункт "О подходе", но это отдельная задача (меню зависит ещё и от Export/Очистить дерево/Horizon-пункта, которых тоже пока нет как единого dropdown).
- Пересмотреть при: если понадобится клиентская интерактивность на странице (например, счётчик прочтений, анимации при скролле) — тогда часть страницы придётся выделить в клиентский компонент, но сейчас в этом нет необходимости.

**D34. PostHog SDK + событийная аналитика + feature flags — все три в одном проходе, т.к. это одна и та же инфраструктура.**
- Rationale: core CJM полностью собран (Auth, персистентность, весь component library) — дальше двигаться вслепую, не видя как реально им пользуются, стало главным риском. §12 (event taxonomy) был полностью специфицирован с самого начала, но ни одно событие не отправлялось. `@dnd-kit`/mobile responsive сознательно остаются ниже приоритетом — §20 явно говорит "фокус сначала на desktop launch", а PostHog blocking для Phase 4 (funnels/retention dashboards нельзя строить без данных).
- Реализовано:
  - `posthog-js` (пакет уже включает `posthog-js/react` как подпуть — отдельный npm-пакет `posthog-js/react` ставить не нужно).
  - `src/lib/posthog/PostHogProvider.tsx` — инициализация в `useEffect` (не на верхнем уровне модуля — `posthog.init()` трогает `window`/`document`, что упало бы при SSR-исполнении client-компонента), no-op если `NEXT_PUBLIC_POSTHOG_KEY` не задан (фаундер ещё не создал PostHog-проект — см. ниже). `person_profiles: 'identified_only'` — анонимный трафик не создаёт полный person-профиль (дешевле). Pageview трекается вручную (`$pageview` при смене `pathname`/`searchParams`) — у App Router нет router-level page-change события, в отличие от Pages Router.
  - `PostHogPageview` (использует `useSearchParams`) обёрнут в `<Suspense>` — иначе весь App принудительно стал бы динамическим; билд подтверждает `/about` остался `○ (Static)` после этого изменения.
  - `src/lib/posthog/capture.ts` — единая точка вызова `posthog.capture()`, без дополнительной логики (пока).
  - **identify/reset** — в `TreeApp`'s auth-effect: `identify(userId, {email})` при входе, но `reset()` вызывается ТОЛЬКО при реальном переходе signed-in → signed-out (через `prevUserIdRef`), не на каждом анонимном маунте — иначе каждый визит незалогиненного человека получал бы новый anonymous distinct_id, ломая кросс-сессионный трекинг для тех кто никогда не логинится.
  - Событий добавлено по §12: `app_opened`, `welcome_shown`/`welcome_closed` (с `time_spent_sec`), `zone_info_clicked`, `example_viewed`, `first_item_added` (props: `zone`; проверка `length === 0` перед вставкой — прямо в `useTreeStore`), `horizon_dialog_shown`/`horizon_chosen`/`horizon_skipped`, `item_added`/`item_edited`/`item_deleted`/`item_dragged` (все с `zone`, `item_dragged` ещё и с `distance_pct`)/`fruit_harvested`(`time_since_added_days`)/`fruit_unharvested`, `questions_drawer_opened`/`questions_zone_viewed`/`questions_level_expanded`, `about_page_opened`/`coach_link_clicked` (два маленьких клиентских "острова" — `PageViewTracker`/`CoachLink` — внутри иначе полностью статичного `/about`, см. D33).
  - Что НЕ сделано в этот проход: `tree_returned` (нужен доп. локальный таймстемп последнего визита — не критично, отдельная задача), granular auth-funnel события внутри самих Server Actions (`signInWithEmail` и т.д.) — вместо этого auth отслеживается косвенно через identify при появлении `userId`.
  - **Feature flags:** механизм — `posthog-js/react`'s `useFeatureFlagEnabled`/`<PostHogFeature>` (уже доступны благодаря тому же `PostHogProviderWrapper`). Ни одного флага не создано — гейтить пока нечего (L10: инфраструктура готова с первого дня, активация — по данным и когда появится реальная premium-фича). Не стали изобретать фиктивный флаг ради "доказательства", что механизм работает — сам факт что `PostHogProvider` подключен уже достаточен.
- **Автозахват отключён в коде, не через дашборд-тоггл.** Живой тест (фаундер, первые события в PostHog Activity) показал вперемешку с нашими именованными событиями кучу шума от PostHog по умолчанию: `Pageview` (дубль нашего ручного `$pageview`), `Web vitals`, и autocapture-события вида `clicked button with text "Сохранить"`/`changed input`/`clicked span with text "Ресурс"`. Это и была причина путаницы с "непонятными названиями" — не сами наши события, а то, что PostHog по умолчанию включает autocapture/pageleave/performance capture поверх них.
  - Явно выставлено в `posthog.init()` (`src/lib/posthog/PostHogProvider.tsx`): `autocapture: false`, `capture_pageleave: false`, `capture_performance: false` (убирает `$web_vitals`), и **`disable_session_recording: true`**.
  - Session recording выключен НЕ из-за шума в логах, а по privacy-принципу №1 (§2): содержимое дерева глубоко личное, session replay буквально записывал бы экран. Сознательно зафиксировано в коде, а не оставлено на дашборд-тоггл, который можно случайно переключить обратно из UI без ревью кода.
  - Итог: в PostHog теперь попадают только события из явных вызовов `capture()` — ничего сверх того, что описано в §12.
- **`item_dragged` изначально стрелял на каждый `mousemove` во время перетаскивания (десятки событий за один драг), а не один раз за жест.** Причина: `capture()` изначально жил внутри стора `dragItem`, который вызывается непрерывно из `useDraggable`'s `handleMove` для живого обновления позиции. Исправлено: `useDraggable` (`src/components/tree/useDraggable.ts`) получил отдельный колбэк `onDragEnd(id, distancePct)`, вызываемый **один раз**, на `mouseup`, с посчитанной внутри хука дистанцией (старт/конец в процентах); `capture("item_dragged", ...)` теперь вызывается из компонентов (`RootItem`/`TrunkItemView`/`FruitItem`), не из стора — `dragItem` в сторе снова только про данные (позиция + Supabase-синк), без аналитики.
- **`item_added` — новое событие, фиксирует КАЖДОЕ добавление (не только первое).** `first_root_added`/`first_trunk_added`/etc. по-прежнему фиксировали только первый элемент в зоне (как было в §12) — этого недостаточно, чтобы понять глубину использования зон со временем (например "человек добавляет только плоды, ствол не трогает"). `item_added` с `{zone}` дополняет это, фиксируя каждое добавление в любую зону. Добавлено по запросу фаундера, не было в исходном §12 — реальный, а не гипотетический пробел, всплывший при первом же живом просмотре событий в PostHog.
- **`first_root_added`/`first_trunk_added`/`first_branch_added`/`first_fruit_added` объединены в одно `first_item_added` с `{zone}`.** Изначальный §12-спек определял их как 4 разных имени события — рабочая инконсистентность с паттерном, который сложился для `item_added`/`item_edited`/`item_deleted`/`item_dragged` (одно имя + `zone`-property, а не N вариантов имени). Фаундер заметил несоответствие при живом просмотре событий; решили унифицировать, а не сохранять исходный §12-вариант ради формальной верности спеку — §12 обновлён под фактическую реализацию (это тот случай, когда "если в коде иначе чем в документе — либо чинить код, либо обновлять документ", и здесь правильнее было обновить документ).
- Фаундеру нужно сделать (аналогично Resend/D28): создать проект на posthog.com (US или EU cloud — учитывая что Supabase уже в eu-central-1/Frankfurt, EU cloud держит данные в одном регионе, но это не жёсткое требование), взять Project API Key из Project Settings, добавить `NEXT_PUBLIC_POSTHOG_KEY` + `NEXT_PUBLIC_POSTHOG_HOST` в `.env.local` и в Vercel env vars (все три environment — Production/Preview/Development). ✅ Сделано 2026-07-20 (проект на EU cloud).
- Пересмотреть при: если объём событий приблизится к бесплатному лимиту (1M/мес) — item_dragged теперь один раз за жест, а не за тик, так что это уже не главный кандидат на прореживание; смотреть по реальным цифрам когда наберётся трафик.

**D35. Error tracking через PostHog Errors, не Sentry — сделано сразу за аналитикой, раньше чем §17 Phase 2 планировал.**
- Rationale: §9 Tech Stack оставлял выбор открытым ("Sentry (free tier) или PostHog Errors"). PostHog Errors не требует нового аккаунта/дашборда — тот же клиент, что уже настроен для аналитики (D34). До этого момента любая ошибка (сломанный Supabase-запрос, необработанное исключение) была невидима никому кроме того, кто в этот момент смотрел в devtools. Учитывая что реальные Auth-аккаунты и Supabase-запись уже в проде (не гипотетически), это была активная слепая зона, а не задел на будущее.
- Реализовано:
  - `capture_exceptions: true` в `posthog.init()` (`src/lib/posthog/PostHogProvider.tsx`) — ловит необработанные ошибки/promise rejections на уровне окна автоматически.
  - `<PostHogErrorBoundary>` (`posthog-js/react`) оборачивает `{children}` внутри `PHProvider` — ловит React render-ошибки, которые autocapture на уровне window не всегда видит (React перехватывает часть ошибок сам). Fallback — `src/components/ErrorFallback.tsx`, намеренно спокойный текст ("Что-то пошло не так" + "Дерево сохранено — ничего не потеряется"), без "!!!" и алармизма — в инструменте для рефлексии ошибка не должна читаться как "ты потерял свои данные" (голос продукта, §1).
  - `reportError(error, context)` — новый хелпер в `src/lib/posthog/capture.ts`, вызывает и `console.error` (для локальной разработки), и `posthog.captureException`. Заменил ВСЕ `.catch((e) => console.error(...))` в `useTreeStore.ts` — ошибки Supabase-записи (insert/update/delete/drag/harvest/horizon, миграция дерева) раньше были видны только в консоли браузера, теперь долетают до PostHog.
- **Ограничение на момент D35 (закрыто в D46 — см. ниже): покрывало только client-side.** Server Actions и `middleware.ts` были невидимы для ошибок — исправлено отдельным, осознанным шагом перед Phase 3, не мимоходом.
- Пересмотреть при: если Server Actions/middleware начнут давать необъяснимые сбои, которые не видно ни в client-логах, ни в Vercel's собственных runtime logs — тогда добавить `posthog-node` или мигрировать на Sentry целиком.

**D36. SEO metadata, OpenGraph image, 404/500 pages, sitemap/robots — Phase 2 code-only items, взяты раньше домена/legal/performance-audit пунктов той же фазы.**
- Rationale: с завершением Phase 1 (component library, auth, data layer, analytics, error tracking) следующий по важности блок — Phase 2 "Launch readiness". Внутри неё есть пункты, требующие founder-действий вне кода (домен → Vercel DNS, legal-тексты, cookie-banner решение зависит от юридической консультации) и пункты, полностью реализуемые в коде без внешних зависимостей — выбраны последние, чтобы не блокироваться на founder's dashboard-время.
- Реализовано:
  - `src/app/layout.tsx` — `metadataBase` (указывает на `rootsnfroots.com`, домен ещё не приземлён на Vercel на момент этого прохода, но `metadataBase` не требует, чтобы домен уже резолвился — используется Next'ом только для построения абсолютных URL в OG/Twitter-тегах), `openGraph`/`twitter` (`summary_large_image`) блоки.
  - `src/app/opengraph-image.tsx` — динамическая OG-картинка через `next/og`'s `ImageResponse` (Satori-рендерер). **Важная деталь, зеркалящая D24:** у Satori нет встроенных кириллических глифов — без явно переданного font-buffer текст на кириллице рендерится как пустые прямоугольники (tofu). Решение — тот же паттерн, что и везде в проекте с шрифтами (см. D24), только на уровне рантайма, не билда: `loadTitleFont()` фетчит Google Fonts CSS2 API с `text=`-параметром (только реально используемые глифы, не весь шрифт) для `Literata:wght@500`, парсит URL шрифта из CSS, фетчит сам файл, передаёт `ArrayBuffer` в `ImageResponse`'s `fonts`. Проверено визуально (не только "билд прошёл") — сгенерированная картинка открыта и кириллица отрендерилась корректно, не как tofu.
  - `src/app/not-found.tsx` (404) — тот же спокойный голос и Tailwind-паттерн, что уже проверен в `ErrorFallback.tsx` (D35): без "!!!", без алармизма.
  - `src/app/error.tsx` — Next.js route-segment error boundary (обязательно `"use client"` по конвенции фреймворка). Ошибки репортятся через `reportError()` (тот же хелпер, что и D35), плюс `reset()` для повторной попытки без полной перезагрузки страницы.
  - `src/app/global-error.tsx` — отдельный, более примитивный fallback конкретно для ошибок в самом root layout (единственное место, которое `error.tsx` не может поймать — оно оборачивает то, что рендерит layout как `children`, но не сам layout). Обязан объявлять собственные `<html>`/`<body>` и заново импортировать `globals.css`, т.к. полностью подменяет root layout, пока активен. Сознательно не использует `font-serif`/next-font CSS-переменные — те объявлены через `className` на `<html>` в обычном `layout.tsx`, которого в этом дереве нет.
  - `src/app/sitemap.ts` / `src/app/robots.ts` — Next.js file-convention routes (не статичные файлы в `public/`), `robots.ts` явно исключает `/auth/*` из индексации.
  - **Три слоя error-обработки теперь чётко разделены по ответственности:** `PostHogErrorBoundary` (D35, в `layout.tsx`, оборачивает то, что рендерит Next как `{children}` — то есть в том числе `error.tsx`'s boundary) → `error.tsx` (route-segment ошибки, ближайший к месту сбоя, с `reset()`) → `global-error.tsx` (ошибки в самом layout, самый внешний и самый примитивный fallback). Порядок перехвата — от внутреннего к внешнему; каждый слой ловит то, что не поймал более внутренний.
- Пересмотреть при: когда домен реально приземлится на Vercel (Phase 2, ещё не сделано) — проверить, что `metadataBase`/`sitemap`/`robots`'s захардкоженный `https://rootsnfroots.com` действительно резолвится и OG-превью корректно подтягивается в реальных шерах (Telegram/Twitter/etc), не только локально.

**D37. `tree.png` (7.5MB, 2858×2509px PNG) заменён на два JPEG-варианта — Phase 2 "Performance audit (Lighthouse)" вскрыл реальную, не гипотетическую проблему, не просто чек-лист-формальность.**
- Контекст: первый прогон Lighthouse против прод-билда показал LCP 42 секунды и общий вес страницы 7.9MB — оба числа биты доминировались одним файлом. `public/tree.png` рендерится в двух местах (`src/components/tree/TreeScene.tsx`): как CSS `background-image` под `backdrop-blur(30px)` (весь фон сцены) и как обычный `<img>`, ограниченный `max-h-[calc(100vh-80px)]` — то есть источник на диске (2858×2509, полный алгоритмический PNG без сжатия) был на порядок больше, чем что-либо из этого реально показывает пользователю.
- Реализовано:
  - Источник разделён на два файла под разные use-case: `public/tree.jpg` (2400×2107, JPEG q90, ~1.2MB) для чёткого переднего `<img>`, и отдельный `public/tree-bg.jpg` (500px по большей стороне, JPEG q80, ~90KB) специально для размытого CSS-фона — раз слой всё равно проходит через `blur(30px)`, детализация источника там роли не играет, малое разрешение после блюра визуально неотличимо от полного. `sips` (родной macOS-инструмент) подтвердил `hasAlpha: no` — картинка непрозрачная, PNG никогда не был нужен как формат, JPEG корректен.
  - `<img>` получил `fetchPriority="high"` и класс `aspect-[2400/2107]` (**не** HTML-атрибуты `width`/`height`).
  - `<link rel="preload" as="image" href="/tree-bg.jpg" fetchPriority="high">` добавлен не в `layout.tsx`, а прямо в `TreeScene.tsx` — React 19 поднимает `<link>`-теги в `<head>` независимо от того, где в дереве компонентов они отрендерены, так что предзагрузка не должна жить в корневом layout ради одной сцены, которая есть только на `/`.
  - Итог измерен, не предполагался: `next build` (не `next dev`) → `next start` → `npx lighthouse` c реальным `Google Chrome.app` через `CHROME_PATH`. Байтовый вес страницы упал с 7.9MB до ~1.8MB, Total Blocking Time с 330ms до 0-30ms.
- **Важное методологическое открытие: Lighthouse по умолчанию эмулирует мобильный viewport и throttling.** Первые прогоны после оптимизации картинки всё ещё показывали LCP ~10s и подозрительно узкий `boundingRect` (72px) у элемента-кандидата на LCP — это не регрессия, а следствие того, что мобильная адаптивная раскладка сознательно ещё не сделана (см. §17 Phase 1, "Базовая mobile responsive раскладка" всё ещё не отмечена, §20 "фокус сначала на desktop launch"). Прогон с `--preset=desktop` (соответствует реальному текущему таргету) показал куда более представительную картину: Performance 92/100, LCP 1.9s (уже в "good"-диапазоне), TBT 0ms. **Урок на будущее (см. также L15): гонять `lighthouse` без `--preset=desktop` для страницы, у которой mobile-раскладка ещё не сделана — вводит в заблуждение, не диагностирует.**
- **Реальный баг в первой версии этого фикса, пойманный фаундером визуально, не мной:** первая попытка закрыть Lighthouse's `unsized-images` audit добавила на `<img>` HTML-атрибуты `width={2400} height={2107}` напрямую. Фаундер заметил, что дерево стало выглядеть "растянутым" — не субъективно, подтверждено измерением: скриншот до/после в headless Chrome при одинаковом viewport (1600×900) показал, что рендер-бокс картинки расширился с ~934px до ~1220px по ширине при той же высоте ~818px (ratio 1.14 → 1.49). Причина: HTML `width`/`height`-атрибуты на `<img>` маппятся в CSS как *definite* (хоть и низкоприоритетные) значения `width`/`height`, а не просто hint для aspect-ratio. Когда оба уже definite, `max-w-full`/`max-h-[calc(100vh-80px)]` (Tailwind) клэмпят каждую ось **независимо**, а не пропорционально — ровно это и растягивало картинку. Фикс: убрать HTML-атрибуты `width`/`height`, вместо них — CSS `aspect-ratio` (Tailwind-класс `aspect-[2400/2107]`), которая связывает оси между собой правильно при том же max-w/max-h. Перепроверено скриншотами (геометрия совпала с оригиналом день-в-день) и Lighthouse (`--preset=desktop`: те же 92/100, LCP 1.9s, CLS 0 — фикс ничего не стоил с точки зрения перформанса). **Урок: `width`/`height`-атрибуты на `<img>`, ограниченном одновременно `max-width` и `max-height` с разными по смыслу единицами (`%` и `calc(100vh-...)`), могут молча сломать пропорции — для отзывчивых изображений использовать CSS `aspect-ratio`, не HTML-атрибуты, если элемент уже зажат парой независимых max-constraints.**
- **Не сделано намеренно:** `color-contrast` audit (score 0) поймал `--ink-muted` (#8a877e) и `--accent` (#6b8fa8) на фоне `--cream` (#fdfbf7) — оба ниже WCAG AA 4.5:1 для мелкого текста. Это прямое следствие design-принципа §6 (приглушённая, землистая палитра, L4 — сознательный отказ от ярких/сатурированных цветов). Не поменяно в этот проход — смена этих hex-значений это design-token решение с visual trade-off, не chore, требует явного founder-решения (принять как есть или скорректировать конкретные токены), не unilateral код-фикс.
- Пересмотреть при: если позже добавится `sharp` как зависимость (например, ради `next/image` для foreground `<img>` — сейчас сознательно plain `<img>`, см. комментарий в коде про drag-математику) — тогда можно рассмотреть WebP/AVIF через встроенный Next Image Optimization вместо статичных JPEG-файлов, дальнейшее сжатие без потери контроля над DOM-структурой, от которой зависит drag.

**D38. `rootsnfroots.com` направлен на Vercel; cookie banner сознательно НЕ делаем сейчас — но это условное решение, не постоянное.**
- Домен: A-запись (`@` → `76.76.21.21`, позже фаундер добавил и рекомендованную Vercel-ом пару `216.198.79.1`/`64.29.17.1`) добавлена в Namecheap Advanced DNS, nameservers не тронуты (Resend'а DKIM/SPF/MX там же, см. D28). SSL — Let's Encrypt, автоматически через Vercel; первый прогон завис дольше обычного (~час, `curl` падал с `SSL_ERROR_SYSCALL`, хотя `vercel domains verify` уже показывал `"ok": true`) — разрешилось нажатием "Refresh" в Vercel dashboard на странице Domains, не потребовало ничего чинить в DNS/коде. Живой тест подтвердил: `200 OK`, валидный сертификат (`Let's Encrypt`, `notAfter` через 3 месяца), HSTS-заголовок присутствует.
- **Cookie banner: решено пока НЕ добавлять.** Rationale: PostHog уже сконфигурирован без session recording, без autocapture, без рекламных пикселей (D34/D35) — единственные cookies сейчас это (а) Supabase auth-сессия — "strictly necessary" по GDPR, согласие не требуется, только раскрытие в Privacy Policy, и (б) PostHog analytics (EU cloud) — формально требует consent под строгим прочтением GDPR, но при текущем масштабе (пре-запуск, founder + горстка тестеров) риск правоприменения нулевой. Добавлять баннер сейчас — trade-off не в пользу пользователя (лишний UI-элемент, product principle #3 "no popups") при отсутствии реальной необходимости.
- **Обязательное условие этого решения, не опция:** будущая Privacy Policy (§17 Phase 2, ещё не написана) ДОЛЖНА явно раскрывать оба сборщика данных — Supabase auth (email, сессия) и PostHog analytics (события из §12, EU cloud, session recording выключен) — это не "если будет время", а прямое условие, при котором cookie-banner-less подход остаётся допустимым.
- **Триггер на пересмотр — зафиксировать явно, не полагаться на память:** если в продукт добавляются (a) реклама/рекламные пиксели, (b) любой сторонний трекер помимо PostHog, (c) session recording когда-либо включается обратно, (d) PostHog autocapture когда-либо включается обратно — тогда cookie banner с явным consent становится обязательным, не опциональным, и это нужно сделать ДО того как новый трекер реально начнёт собирать данные, не после.
- Пересмотреть при: любом из четырёх триггеров выше, или при первом реальном запуске рекламы/маркетинговых интеграций (см. теперь D44 — уже не полное исключение, а точный скоуп).

**D39. Юридический статус — гражданин РФ, не регистрирует юр.лицо в РФ никогда; будущее Romanian SRL по триггеру.**
- Фаундер — гражданин РФ, проживает в Молдове, в течение года планирует переезд в Румынию, также имеет румынское гражданство.
- Не регистрирует ИП/юр.лицо в РФ — никогда, ни при каких условиях.
- План: открыть Romanian SRL (как гражданин ЕС), когда наступит один из триггеров — (a) ~1000+ активных пользователей, (b) начался revenue через premium-подписку, (c) нужно юр.лицо для партнёрств. Не раньше.
- До этого момента сервис юридически ведёт физическое лицо. Идентификация присутствует ТОЛЬКО внутри текста Privacy Policy и Terms of Service — НЕ в видимом футере сайта. Футер держит только ссылки "Конфиденциальность · Условия" (реализовано — `RightPanel.tsx`, без имени/юр.статуса в UI).
- Явно НЕ заполнять RF-специфичные поля (ИНН, ОГРН, российский юридический адрес) нигде на сайте — оставлять пустыми/неприменимыми, не подбирать эквивалент.
- Реализовано: `/privacy` — "Veaceslav Barbanov, физическое лицо, проживающее в Молдове" в разделе "Кто обрабатывает данные". `/terms` — раздел "Применимое право": на переходный период (физлицо, ранний доступ) действует законодательство и суды Республики Молдова (по месту фактического проживания), с явной оговоркой в тексте, что после регистрации Romanian SRL применимое право обновится на румынское — сигнализирует пользователю честный ранний статус сервиса, а не скрывает его.
- **Реальная юридическая проверка обоих документов сознательно отложена, не блокирует запуск.** Триггер на проверку — любое из: (a) старт монетизации (Stripe/premium), (b) регистрация SRL. До этого момента `/privacy` и `/terms` — компетентный черновик по фактическому устройству продукта, не юридически вычитанный документ.
- Пересмотреть при: наступлении любого из трёх триггеров из плана SRL выше (тогда — реальные данные Romanian SRL, применимое право → Румыния) ИЛИ триггеров юридической проверки в пункте выше — какой наступит раньше.

**D40. Инфраструктура остаётся в ЕС независимо от языка контента — постоянное решение, подтверждает и уточняет D25.**
- Vercel + Supabase (EU/Frankfurt) — постоянно. Уже так с самого начала (см. D22 про выбор региона), это решение просто явно фиксирует, что смена языка/аудитории (D42 ниже) НЕ означает смену инфраструктуры — два вопроса независимы.
- Никакого российского хостинга, никогда никакого `.ru`-домена. Rationale: риск санкций ломает интеграции Stripe/GitHub/PostHog; язык контента ≠ место хостинга, эти два не должны считаться связанными при любых будущих решениях.
- Состояние на старте (без изменений от D25): `rootsnfroots.com` (корень, без префикса) = русский. Английский добавится позже на `/en` (уточняет D25 — конкретное имя интерим-пути до свопа).
- Пост-swap состояние (без изменений от D25): когда английская версия готова к полноценному использованию — swap через 301 одномоментно, корень становится английским, `/ru` — русский с префиксом.

**D41. Privacy Policy — явная формулировка про местоположение данных (GDPR-стиль).**
- Требование: явная фраза вида "твои данные хранятся на серверах в Евросоюзе (Германия), которые обслуживает Supabase" — юридический текст, не маркетинг, не выносится в UI как акцент.
- Реализовано: `PRIVACY_TEXT` (`src/lib/tree/copy.ts`) уже содержит эквивалент — "Supabase — хранение аккаунта и содержимого дерева (сервер в Европе, Франкфурт)" в разделе "С кем мы делимся данными". Франкфурт однозначно в Германии — формулировка уже соответствует духу требования, отдельного изменения не потребовалось.

**D42. Bilingual roadmap — уточняет D25, не меняет план.**
- Запуск полностью на русском (Phases 1-4), без изменений.
- После валидации русской версии (~3-6 недель после запуска, по реальному фидбэку и метрикам) — начинается английская **адаптация**, не перевод: тон, примеры, раздел "От автора" переосмысливаются культурно, не переводятся подстрочно.
- Английский контент первично живёт на `/en` — swap на корень происходит позже, когда английская аудитория/трафик оправдает это (см. D40/D25 про сам механизм свопа).

**D43. Monetization — план подтверждён, дополнен опцией нативной рекламы (см. D44).**
- Бесплатно на старте. Premium активируется после валидации PMF (WAR > 20%, ~1000 активных пользователей) — без изменений от §13/D16.
- Модель уточняется: **Freemium + подписка + опциональная нативная реклама в периферийных зонах** (было просто "Freemium + подписка", см. D44 для деталей рекламы). Цена — ориентир $4.99/мес или $29.99/год, с локализацией под рынок, без изменений.
- Явно НЕ делаем: одноразовые платежи (one-time payments) — без изменений от §13.
- §13 "Не делаем" обновлён (см. правку раздела) — было безусловное исключение рекламы, стало точный скоуп через D44.

**D44. Реклама — уточнённая позиция: не полное исключение (как было в §13 до этого момента), а точный скоуп по зонам.**
- НЕ в основном flow дерева (само дерево, вопросы для рефлексии, диалоги) — это "sacred space" продукта (product principles #2/#4, §2) — реклама там сломает wellness-атмосферу, ради которой продукт вообще существует.
- Допустима нативная/партнёрская реклама в периферийных зонах — низ страницы "О подходе", будущий блог, лендинг — и только релевантный контент (книги, смежные курсы, смежные приложения), не произвольные рекламные сети.
- Отключена для premium-подписчиков — дополнительный стимул апгрейда, помимо самих premium-фич.
- Не активируется раньше Phase 4-5 (после валидации продукта) — см. §17 roadmap.
- §13 "Не делаем" обновлён этой правкой — старый пункт "❌ Реклама" заменён на явное разграничение "не в core flow / можно периферийно, с ограничениями".

**D45. Mobile-стратегия — PWA как Phase 6-7, native — долгосрочная опция, не ближний приоритет.**
- Web-first запуск — без изменений от текущего плана.
- PWA (installable на home screen, push notifications, offline-capable) — новые Phase 6-7 в §17 roadmap, после Iterate/Monetization. Покрывает по оценке фаундера ~70% ценности native-приложения за ~5% работы.
- Native iOS/Android — долгосрочная возможность, не ближний приоритет. Триггер на пересмотр: значимый mobile-трафик + PWA упирается в реальные платформенные ограничения.
- Архитектурное требование (не новое, просто явно подтверждено): ничего в текущей архитектуре не должно закрывать путь к PWA позже — service worker capability, responsive design с первого дня остаются принципом (§2 п.6), независимо от того, что сама мобильная раскладка как отдельная задача всё ещё в бэклоге Phase 1 (см. §17, D37/L15 про то, что это сознательно отложено, не забыто).
- Закрывает §19 Open Question №5 ("Mobile стратегия — TBD когда переключаться на native") — см. правку того раздела.

**D46. Server-side error tracking через `posthog-node` — последний пункт Phase 2, сделан ровно в момент триггера D35 (перед Phase 3), не раньше и не позже.**
- Rationale: D35 явно отложил это до "перед Phase 3, не раньше" — фаундер подтвердил готовность к Phase 3, это и есть момент триггера.
- Реализовано:
  - `src/lib/posthog/server.ts` — `reportServerError(error, context)`, использует `posthog-node` с тем же project API key, что и клиент (`NEXT_PUBLIC_POSTHOG_KEY` — это write-only ingest-ключ, не секрет, в отличие от Personal API Key из §19). `captureExceptionImmediate` (не `captureException`) и `flushAt: 1, flushInterval: 0` — сериверлесс-функция на Vercel может завершиться до того, как сработает обычный batched-flush; awaited immediate-вариант гарантирует отправку до возврата ответа.
  - Обёрнуты: `signInWithEmail`, `signInWithGoogle`, `signOut` (`src/app/auth/actions.ts`), `exchangeCodeForSession`-ошибка и missing-code случай в `/auth/callback` (`route.ts`), session-refresh в `src/lib/supabase/middleware.ts` (обёрнут в try/catch — ошибка Supabase здесь не должна ронять весь сайт, это выполняется на каждом запросе).
  - **Сознательно НЕ обёрнуто:** `confirmMagicLink`'s `if (error)` ветка (истёкший/использованный token) — это ожидаемое, рутинное трение (email-scanner боты сжигают токены прематурно, см. D27), не баг. Репортить каждый такой случай как exception — тот же класс шума, что чинили в D34 (item_dragged-per-tick, autocapture). Аналогично не репортится `!email`/`!tokenHash` — это client-side validation gaps, не серверные сбои.
  - Redirect-safety: НЕ оборачивали функции целиком в try/catch — `redirect()` в Next.js бросает специальную ошибку с `digest` вида `NEXT_REDIRECT`, которую нельзя проглатывать; `isRedirectError` существует только как internal API (`next/dist/client/components/redirect-error`), не публичный экспорт. Вместо этого репорт добавлен точечно, внутри уже существующих `if (error)`-веток — там, где Next-throw ещё не произошёл.
  - Проверено структурно (не через PostHog dashboard — Personal API Key ещё не создан, см. §19 п.7): `curl` на `/auth/callback` без `code` — редирект по-прежнему корректный (307), `reportServerError` отработал без зависаний/необработанных исключений, `console.error` вывел ожидаемый текст.
- **Побочная находка в этом же проходе — не связана с error tracking, но реальная:** `npm audit` (при установке `posthog-node`) вскрыл несколько высокого уровня CVE в `next@16.2.10` (middleware/proxy bypass, SSRF в Server Actions и rewrites, DoS, unauthenticated disclosure server function endpoints) — все исправлены патч-версией `16.2.11`. Обновили пин (`package.json`, всё ещё точный пин, не диапазон — см. §9 принцип 5, апдейт безопасности это ровно тот случай когда пин нужно двигать). Билд/тесты прошли чисто. Отдельно: `sharp` всё ещё флагуется audit'ом, но это внутренняя зависимость Next'а собственного Image Optimization API (не то, что мы вызываем — см. D23 про сознательный отказ от `next/image` для `<img>` дерева, и OG-картинка через Satori, не sharp); npm предлагет как "фикс" даунгрейд Next до v9 — проигнорировано как абсурдное, ждём апстрим-фикса от самого Next.js.
- Пересмотреть при: если объём server-side ошибок станет значимым и понадобится более глубокая трассировка (stack traces через несколько serverless-инстансов, correlation с client-side ошибками того же юзера) — тогда рассмотреть полный переход на `@sentry/nextjs` (единый SDK client+server), как и предполагал D35 изначально.

---

## 17. Roadmap to Launch

### Phase 0: Setup (week 1)

- [x] Прототип в чате — UX, тексты, лука, поток
- [x] Локальный setup: Node, Claude Code, project folder
- [x] GitHub repo создать, первый коммит — [SnowVB/rootsnfroots](https://github.com/SnowVB/rootsnfroots)
- [x] Next.js app scaffolding
- [x] Supabase project create — проект создан (`mvfffajijkqyakqlpuyt`), ключи в `.env.local`; schema setup — следующий шаг
- [x] Vercel proект create, GitHub-connect
- [ ] PostHog project, API keys в env
- [x] CLAUDE.md в репо (этот документ)

### Phase 1: MVP migration (weeks 2-4)

- [x] Перенос дизайн-системы из прототипа в Tailwind config — цвета/шрифты в `src/app/globals.css` (`@theme`), Manrope/Literata через `next/font/google` в `src/app/layout.tsx` (Literata вместо Fraunces — см. D24)
- [x] Component library: полностью готов — ~~Root~~, ~~TrunkItem~~, ~~BranchItem~~, ~~FruitItem~~, ~~AddButton~~, ~~InfoPopover~~, ~~ExampleModal~~, ~~WelcomeModal~~, ~~HorizonDialog~~, ~~QuestionsDrawer~~ (`src/components/tree/`, см. D23, D30, D31, D32) + ~~AboutPage~~ как реальный роут `/about`, не модалка (см. D33)
- [x] Data layer: Supabase schema + RLS policies + типы — `supabase/migrations/0001_init.sql`, типы в `src/lib/supabase/database.types.ts`
- [x] Auth: Supabase (email magic link и Google OAuth) — оба подтверждены рабочим живым тестом фаундера. Magic link потребовал фикса (D27) и custom SMTP через Resend (D28); Google OAuth работает через Client ID/Secret в Supabase dashboard, см. D26
- [ ] Drag & Drop: @dnd-kit integration
- [x] Routing: `/` (tree), `/about` (как страница) — см. D33
- [x] Анонимная сессия → миграция в БД при signup — `src/lib/supabase/tree.ts` + `useTreeStore` (`initForUser`/`clearUser`), см. D29
- [x] PostHog SDK + event tracking (по спеке из раздела 12) — код готов, ждёт от фаундера создания PostHog-проекта и env vars, см. D34
- [x] Feature flags структура (но всё включено) — механизм готов через `posthog-js/react` (`useFeatureFlagEnabled`), флагов пока нет — нечего гейтить, см. D34
- [ ] Базовая mobile responsive раскладка

### Phase 2: Launch readiness (week 5)

- [x] Домен `rootsnfroots.com` направлен на Vercel — A-запись на Namecheap (Advanced DNS, не тронули nameservers — Resend'а DKIM/SPF/MX там же, см. D28), SSL (Let's Encrypt, через Vercel) выпущен, HTTPS подтверждён живым тестом
- [x] SEO: meta tags, OpenGraph для шеринга — `metadataBase`/OpenGraph/Twitter card в `src/app/layout.tsx`, динамическая OG-картинка `src/app/opengraph-image.tsx`, см. D36
- [x] Базовый legal: Privacy Policy (`/privacy`), Terms of Service (`/terms`) — раскрывают Supabase auth + PostHog analytics (условие D38 для cookie-banner-less подхода выполнено), governing law = Молдова на переходный период (см. D39). Реальная юридическая проверка сознательно отложена до старта монетизации или регистрации SRL (D39) — не блокер запуска.
- [x] Cookie banner — решено пока не делать (см. D38 для rationale и явных триггеров на пересмотр: реклама, сторонние трекеры, включение session recording/autocapture обратно)
- [x] Error tracking (Sentry или PostHog Errors) — выбран PostHog Errors, сделано раньше графика (сразу после аналитики, тот же клиент), см. D35. Server-side покрытие добавлено отдельно, см. следующий пункт и D46
- [x] **Server-side error tracking** — `posthog-node` в Server Actions, `/auth/callback`, middleware. Последний пункт Phase 2 — см. D46.
- [x] 404 page, 500 page — `src/app/not-found.tsx` (404), `src/app/error.tsx` (route-segment runtime errors, with retry), `src/app/global-error.tsx` (root-layout-level fallback), см. D36
- [x] Sitemap.xml, robots.txt — `src/app/sitemap.ts`, `src/app/robots.ts` (Next.js file-convention routes, disallow `/auth/`)
- [x] Performance audit (Lighthouse) — нашёл реальную проблему (`tree.png`, 7.5MB), не формальность. Desktop preset: Performance 92/100, LCP 1.9s. См. D37, L15 (mobile-preset числа для этой страницы сейчас нерепрезентативны — mobile-раскладка ещё в бэклоге)
- [x] Cross-browser testing — Safari, Chrome, Firefox и Яндекс.Браузер (актуален для ru-аудитории, Chromium-based) все проверены фаундером вручную, ок. По пути пойман реальный Safari-only баг (backdrop-filter, см. L13)

### Phase 3: Soft launch (week 6)

- [ ] Деплой в production
- [ ] Vyacheslav анонсирует в Telegram-канале
- [ ] Первые 20-50 пользователей из тёплой аудитории
- [ ] Базовый дашборд в PostHog
- [ ] Сбор feedback (DM, опрос в Telegram)

### Phase 4: Iterate (weeks 7-12)

- [ ] Качественные интервью (10-20)
- [ ] Share card (виральный механизм)
- [ ] Smart reminders (email)
- [ ] Активация feature flags для первых тестов premium-фич
- [ ] PostHog funnels + retention dashboards
- [ ] Weekly metric review (с Vyacheslav)

### Phase 5: Monetization (week 13+, если метрики позволяют)

- [ ] Stripe integration
- [ ] Premium gates активированы
- [ ] Multi-tree premium feature
- [ ] First paying customers
- [ ] Нативная реклама в периферийных зонах (низ "О подходе", будущий блог/лендинг) — опционально, см. D44. Отключена для premium.

### Phase 6-7: Mobile PWA (после Monetization, сроки TBD)

См. D45. Web-first launch не меняется — это отдельный, более поздний трек, не блокер для Phase 1-5.

- [ ] Service worker + `manifest.json` — устанавливаемость на домашний экран
- [ ] Push notifications (web push)
- [ ] Offline-capable core flow (просмотр/редактирование дерева без сети)
- [ ] Оценить необходимость native (iOS/Android) — триггер: значимый mobile-трафик + PWA упирается в реальные ограничения платформы

---

## 18. Lessons Learned (from prototype)

### L1. In-browser Babel — хрупкая зависимость

- **Что произошло:** Новые версии `@babel/standalone` начали вставлять `import` statements в скомпилированный JSX (новый "automatic" JSX runtime). Safari отказывалась парсить это без module-context. Прототип ломался.
- **Решение в прототипе:** Запинили версию `7.24.7` + явно указали `data-presets="react"`.
- **В Production:** Никакого in-browser компилятора. JSX компилируется на build через Next.js (SWC).

### L2. Google Fonts API меняется

- **Что произошло:** URL с `Fraunces:opsz,wght@9..144,400;500;600` начал возвращать 400. Скорее всего временный сбой, но напомнил о хрупкости.
- **Решение в прототипе:** Убрали `opsz` axis, оставили только wght.
- **В Production:** `next/font/google` — оптимизированная локальная подгрузка, без runtime сюрпризов.

### L3. Картинка через `file://` в Chrome перестала работать

- **Что произошло:** Обновление Chrome ужесточило CORS для локальных файлов. `<img src="tree.png">` через file:// перестал грузиться.
- **Решение:** Python local server (`python3 -m http.server`).
- **В Production:** Картинки идут через Next.js Image Optimization, на CDN.

### L4. Цветовая палитра эволюционировала

- **Сначала:** Яркие "crayola" тона (saddleBrown #8B4513, forestGreen #228B22, ярко-зелёный, томатный оранжевый)
- **Стало:** Приглушённые землистые (терракота #B8896B, мох #7A8D6A, sage #AED581, коралл #E8876B)
- **Урок:** В wellness-продуктах сатурация важна. Яркое = детское/нервное. Приглушённое = взрослое/рефлексивное.

### L5. Drag UX требует careful state management

- Click vs drag conflict: пришлось вводить threshold 3px движения чтобы различать
- DeleteBtn внутри draggable требовал stopPropagation на mousedown, иначе родитель открывал edit
- InfoPopover на ту же кнопку требовал anchorRef чтобы корректно toggle'иться

### L6. Welcome-модалка эволюционировала через тестирование

- Старая: одно предложение про "не знают чего хотят"
- Новая: 90 слов с конкретикой + якорь про крону/корни/ствол + про возвращение
- Урок: первый экран нужен **достаточно длинный** чтобы поставить контекст, **не настолько длинный** чтобы отпугнуть.

### L7. Микро-копирайт делает продукт

- "Горизонт" → "Период реализации" — звучит конкретнее
- "Заполняй медленно" → "Заполняй медленно, вдумчиво" — два слова делают работу за абзац
- Слово "наследована" → "возможно желание не лично твоё, а взято извне" — проще, точнее
- Урок: микро-итерации копирайта продолжаются до самого конца. Никогда не считать что "написано окончательно".

### L8. Структура вопросов 3 уровня — паттерн

- "С чего начать / Чтобы копнуть глубже / Тело подсказывает" — рабочий паттерн прогрессии
- Юзер сам выбирает глубину, не давим
- Тело-вопрос — uniqueness нашего метода

### L9. Adding "если отклика нет" — мощная мини-разрешение

- Pattern: в тело-вопросах добавлять *"Если отклика нет — возможно, это не твоё или не актуально"*
- Это **разрешает юзеру отпустить** то, что не работает. Сильное для корней и веток (там часто пишут "правильные" чужие опоры/желания).

### L10. Premium-архитектура с дня 1, активация позже

- Поле `tier: 'free' | 'premium'` в user model сразу
- Feature flags — сразу
- Активация — только после данных и valid PMF
- Урок: предотвращает rewrite. Стоимость "заложить" низкая, стоимость "переделать архитектуру" высокая.

### L11. North Star метрика должна быть не "feel good" а "predictive"

- DAU/MAU — feel good но обманчивы (могут расти на спам-фичах)
- Session duration — anti-pattern в wellness
- WAR (Weekly Active Returners) — predictive: если люди возвращаются, продукт работает

### L12. Каждое решение → в Decision Log

- Без него через 3 месяца не помнишь почему сделано так. Особенно с AI-партнёром, где сессии конечны.
- Decision log = страховка от перепеределывания одного и того же.

### L13. Наложенные backdrop-filter (blur) слои — реальный источник z-index багов; фикс — портал, не isolate

- **Что произошло:** фаундер сообщил (со скриншотом) что `HorizonDialog` рендерился визуально ЗА деревом — несмотря на то, что `z-index` (1800 у диалога против 1 у картинки дерева) был технически верным, и это подтвердилось разбором скомпилированного CSS (`z-index: 1800` реально присутствовал в выводе). Диагностика z-index-значений, вложенности компонентов и структуры JSX не нашла ошибки — код был "правильным на бумаге".
- **Вероятная причина:** в сцене дерева уже есть свой `backdrop-blur` слой (затемнение фона), и модалки (`WelcomeModal`, `ExampleModal`, `HorizonDialog`, `Toast`) добавляют СВОЙ собственный `backdrop-blur` поверх. Несколько соседних/наложенных `backdrop-filter`-элементов — задокументированная категория багов рендеринга в Chrome/Safari, где порядок отрисовки может не совпадать с формально верным z-index, особенно с абсолютным позиционированием.
- **Первая попытка (частичная):** `isolation: isolate` на корневом div каждой модалки/тоста — починило Chrome, **не починило Safari** (фаундер проверил оба). Safari исторически имеет более серьёзные/другие баги композитинга backdrop-filter, чем Chrome — `isolate` тут оказался недостаточным.
- **Итоговый фикс:** `WelcomeModal`, `ExampleModal`, `HorizonDialog`, `Toast` переведены на React Portal (`src/components/tree/Portal.tsx`, `createPortal(children, document.body)`) — рендерятся напрямую в `document.body`, полностью вне DOM-поддерева сцены дерева и её `backdrop-filter`. `position: absolute` → `position: fixed` соответственно (нет позиционированного предка после портала, `fixed` — надёжнее относительно viewport). Это не борьба с CSS stacking context, а полное устранение проблемы: контент физически не является потомком элемента с конфликтующим `backdrop-filter`. Это же стандартный подход всех modal-библиотек (Radix, Headless UI) именно по этой причине.
- **Урок:** для полноэкранных overlay/модалок с `backdrop-filter` — сразу проектировать через портал в `document.body`, не полагаться на z-index/isolation внутри дерева компонентов. `isolate` — не панацея, особенно в Safari.

### L14. "Удалить" в меню плода закрывал меню, но не удалял плод — stopPropagation ненадёжен как единственная защита от outside-click

- **Что произошло:** фаундер заметил, что клик на "Удалить" в мини-меню плода (`FruitItem`) просто закрывал меню, не удаляя сам плод; при этом крестик (`DeleteBtn`), доступный при наведении вне меню, удалял корректно — оба вызывают один и тот же `onDelete(item.id)`.
- **Причина:** `FruitItem`'s outside-click-close эффект слушал `document`'s `mousedown` БЕЗ проверки `e.target` — просто `() => setMenuOpen(false)` на любой клик где угодно, полагаясь на `e.stopPropagation()` в `onMouseDown` кнопок меню, чтобы этот блуждающий closer вообще не срабатывал при клике ВНУТРИ меню. Это оказалось ненадёжным (точный механизм не установлен — возможно нюанс React 19 event delegation vs. нативный `document`-listener) — clic по кнопке "Удалить" всё равно триггерил закрытие меню раньше/вместо чем срабатывал сам `onClick`.
- **Фикс:** `InfoPopover` уже решал ту же задачу правильно — через `ref` на сам popover и явную проверку `!popRef.current.contains(e.target)` внутри closer'а, вообще не полагаясь на stopPropagation для этой части. `FruitItem` переведён на тот же паттерн (`menuRef` + `contains()`-проверка). `stopPropagation()` на контейнере меню оставлен — он всё ещё нужен отдельно, чтобы клики по меню не триггерили drag-обработчик на внешнем div плода.
- **Урок:** для "закрыть при клике вне элемента" — всегда проверять `ref.contains(e.target)` в самом обработчике, не полагаться на `stopPropagation()` дочерних элементов как единственную защиту. Это на порядок надёжнее и уже был проверенный рабочий паттерн в этом же кодбейзе (`InfoPopover`) — стоило сразу скопировать его, а не писать вторую, более хрупкую версию.

### L15. Lighthouse по умолчанию гоняет мобильный профиль (viewport + throttling) — для страницы без mobile-раскладки это вводит в заблуждение, а не диагностирует

- **Что произошло:** после оптимизации `tree.png` (см. D37) LCP по данным Lighthouse всё ещё держался на ~10 секунд, а элемент-кандидат на LCP имел подозрительный `boundingRect` шириной 72px — на первый взгляд похоже на то, что фикс не сработал.
- **Причина:** Lighthouse без флагов эмулирует mobile-viewport и throttled-сеть по умолчанию. Mobile-адаптивная раскладка в этом проекте сознательно ещё не сделана (§17, §20 — "фокус сначала на desktop launch") — то есть Lighthouse честно измерял то, как страница ведёт себя на экране, для которого она пока не спроектирована, а не регрессию в самом фиксе.
- **Фикс:** `lighthouse ... --preset=desktop` — сразу другая, представительная картина (Performance 68→92, LCP 42s→1.9s после того же самого фикса картинки).
- **Урок:** прежде чем доверять числам Lighthouse (или вообще любого performance-инструмента с viewport-профилем по умолчанию) — сверить профиль аудита с реальным текущим таргетом продукта. Тестировать mobile-профилем страницу, у которой mobile-раскладка в бэклоге, а не в проде — гарантированно даёт пугающие, но нерелевантные числа.

---

## 19. Open questions (TBD)

Вопросы, на которые ответим **в процессе** (не сейчас, не в один день):

1. Личная история Вячеслава в "О подходе" — 2-3 предложения от первого лица как добавочный блок (раскрытие "почему именно я сделал это дерево"). Сейчас "От автора" уже включает часть. **Решено отложить до коворка с Obsidian.**
2. Telegram-канал bio: оставлять "practicing mindset coach" или менять на русское — обсудить отдельно, не блокер.
3. Когда именно делать share card — на Phase 4. Триггер: если WAR < 15% — приоритет, без share card не вырастем.
4. Когда подключать AI insights (C3 в бэклоге) — TBD. Возможно только после многих интервью, чтобы понять реально ли нужно.
5. ~~Mobile стратегия~~ — **решено, см. D45.** PWA как Phase 6-7 (§17), native — долгосрочная опция, триггер: значимый mobile-трафик + PWA упирается в ограничения.
6. Auth-письма (magic link) сейчас на английском тексте Supabase по умолчанию ("Your sign-in link" / "Sign in") при русском Sender name ("Дерево Опоры") — несоответствие языка. Не критично пока тестирует только фаундер; перевести на русский до реального запуска (Phase 2/3), заодно можно улучшить дизайн письма (см. также D27 — редактирование шаблона разблокировано только после подключения custom SMTP).
7. Фаундер планирует создать PostHog **Personal API Key** (не тот же самый `phc_...` Project API Key из D34 — Personal Key даёт read-доступ к данным, это чувствительный секрет, не `NEXT_PUBLIC_`), чтобы Claude мог напрямую смотреть события/аналитику через PostHog Query/HogQL API при разборе продуктовых гипотез. Пока не создан — до этого можно передавать данные вручную (скриншоты, экспортированные файлы из PostHog).

---

## 20. Quick Reference (для быстрого старта новой сессии)

**Что строим:** Web app для самопознания через метафору дерева (опоры/ресурсы/ветки/плоды).

**Стек:** Next.js 14 + Tailwind + Supabase + Vercel + PostHog.

**Текущая фаза:** Phase 0 setup, переход из прототипа в production.

**Где артефакты прототипа:** В этом репо в папке `/prototype/` — `index.html`, `tree.png`. Это **референс**, не production-код.

**Кто читает этот документ:** Claude в Claude Code сессиях, Vyacheslav (фаундер, продакт, тестер).

**Что делать НЕ:**
- Не активировать paywall до Phase 5
- Не геймифицировать инсайты
- Не делать push-нотификации до согласованного reminder-flow
- Не менять texts без обновления раздела 7 в этом документе
- Не использовать яркие сатурированные цвета (см. L4)
- Не использовать in-browser компиляторы (см. L1)
- Не делать solo-решения по архитектуре без обсуждения с фаундером

**Что делать ДА:**
- Server-first где можно
- Feature flags для всех premium-фич
- Mobile responsive с первого дня (но фокус сначала на desktop launch)
- Логировать всё в PostHog по схеме из раздела 12
- Обновлять Decision log при каждом значимом решении
- Тестировать в Safari и Chrome перед мерджем

**Контакты владельца:**
- Vyacheslav, @snowvb в Telegram
- GitHub: подключен

---

*Документ обновлён: на момент финализации прототипа (фаза 0). Все последующие изменения — через PR с обновлением этого файла.*
